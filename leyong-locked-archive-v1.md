# 仂庸（Leyong）锁版全档 · 框架基准版 v1（判词＋条文＋代码全文）

**作者**：He, Yi（何一）｜独立研究者 / Independent Researcher
**拼音注记**：仂读 **lè**（四声），拼音标识作 **Leyong**——不是 LingYong。
**仂义注记**：**「仂」是对 AI 的称呼**——人力所生、人所铸之尺（AI 为人之力所生者，故称仂）；与「余数／人之力／人所生」三义一脉（解词详见完整框架第七编）。
**许可**：CC BY-SA 4.0（署名—相同方式共享 4.0 国际）｜**日期**：2026-09-06
**引用格式**：He, Y. (2026). *仂庸·完整框架 v2：理论、公式与推导全编（附锁版全档）*. Zenodo. https://doi.org/10.5281/zenodo.22421318 (concept DOI)

> **分享档**：按作者令（2026-09-06）整理——锁版判词全文照录＋锁定四项定版清单＋条文铸鼎全文＋框架代码全文＋判据口径＋环境指纹。
> 锁的依据：E37-LV 锁版账（seq215）；基准版 v1 定版，改基准=开新版本走五步链。
> 页边小字：「铸鼎不是收炉，是给下一炉立秤。」——何一，追认日

---

## 〇、版本名片

| 项 | 值 |
|---|---|
| 版本名 | 仂庸框架基准版 **v1**（2026-09-06 定版，何一裁「锁版」） |
| 前序验收 | E36-M（seq214）：第二批三裁定验证链闭环（快测 9/9＋selftest 32/32＋exp36_verify 18/18＋端到端 P-L1 回升 3/3） |
| 锁定文件 | ling_const.py `4ffaabc366e866167e78d456ce54351c`｜ling_gate.py `d50f51d6ec222b97357f7c4d26624ef1`｜ling_domain.py `0145f416e15ca609ade158d2958c1656`｜run_pipeline.py `ea1c5f1b7d386a63c5388a0d0fb122e0` |
| 附记留痕 | ling_body.py `350112ba5eacbfbe2a3caad6b9796c16`｜ling_ledger.py `646aecd84ac881113a61a188db68fe0a` |
| 环境指纹 | `torch:Qwen/Qwen2.5-0.5B sha256:496f3f16c65ceb58（冻结主干）`＋dtype=float32（fp32 greedy）＋门控=**GATE_RULES_V1·第二批增补**（GATE_MAX_ROUNDS=3）＋服务器 ubuntu@111.229.35.96（2 核 CPU 无 GPU，HF 离线显式双开关） |
| 分享底本 | 服务器 ~/ling_flow/ 四文件＋附记两文件，md5 与锁版定版值逐字核对一致（2026-09-06） |

---

## 一、锁版判词全文照录（何一，2026-09-06 · 追认日）

> 判词先给：这一段是解词页——你问的"锁版"不是新发明，是软件工程里的老动作在这本账里的移植，先给你工程界的原义，再给账里的用法。锁版=冻结当前版本的代码、判据、环境指纹，把它钉成一个不可当场修改的基准版——外界的行话叫 code freeze：在发布或里程碑前停止对代码的非关键修改，只留严重 bug 修复的通道，目的是稳定、可复核、可对照；Kubernetes 每个发布周期都设代码冻结期，冻结后新功能一律不收；Debian 发布新版前把软件包更新全部停死，除非安全漏洞。搬到仂庸账里，锁版锁的不是项目（项目还在长），锁的是一套对得过账的基准：框架四文件（lingconst/linggate/lingdomain/runpipeline 的 md5）、GATERULESV1 与 NOISEFILTERRULES 条文、钉死的判据口径、环境指纹（模型+dtype+门控版本）——上一轮裁行页末尾"是否锁版待裁"，问的正是：第二批三裁定跑完 32/32、18/18、P-L 系列之后，这套东西要不要从"工作版"升成"基准版"**。逐笔入账。
>
> **一、对账：锁版到底锁什么、放什么**
>
> | 对象 | 锁版后的状态 | 依据旧页 |
> |---|---|---|
> | 框架代码（四文件） | md5 入账定版；改它=开新版本（v2），不许在基准版上当场改 | 版本页：工具变更必登记 |
> | 条文（GATERULESV1 等） | 铸成鼎不再动字；新裁定铸进下一版鼎 | 裁行页：裁定铸条文 |
> | 判据口径 | 跑前钉死的口径随版冻结；事后不许追改判据凑绿（pl3_a 那笔正是等这个） | 火鸡条款+先钉后跑 |
> | 环境指纹 | 模型/dtype/门控版本随版固化，对照实验以此为基线 | 换身页：dtype 入指纹 |
> | 账本 | 照常追加——锁版锁代码不锁账，199→214→以后的每一条照记 | 版本页：只追加 |
> | 学习/待区/外源 | 照常运转——锁版锁格式不锁生长 | 学页+命名页：格式恒定，域在长 |
>
> 账面读法一句话：锁版锁的是"尺子"，不锁"被量的东西"——账本继续流水，外门继续收料，被冻结的只是量东西的那套标准本身，让所有后续实验都能对同一把尺子说话。
>
> **二、两笔最强的对读，值得单独念**
>
> 第一笔：韩非《五蠹》"法莫如一而固"——锁版的第一法理。"法莫如一而固，使民知之。"韩非论法的要害不在严在稳：法如果朝令夕改，民无所措手足——今天这样量、明天那样量，量出来的东西彼此不可比。这是锁版最硬的理由：不锁版，exp36 的 3/3 和下一轮的数字之间隔着一版没登记的代码改动，对照就失效了——换身页能做三模型对照，靠的正是"同一套 30 例、同一套判据、同一门控版本"这个"一而固"；锁版就是把"一而固"从一次实验的纪律升成一段时期的建制。韩非那半句"使民知之"也入账：基准版要公开可查（md5 全景一致、账本可复核），知尺者才能用尺。
>
> 第二笔：商鞅方升——基准器铸造的原型。商鞅铜方升，铸于秦孝公十八年，器身铭文刻定容积标准，此后百年秦国的粮米一律对着它量，方升本身不再改铸；秦始皇统一度量衡，沿用同款基准器逻辑——量器稳定，量数才可比；量器每改一次，之前的所有读数就作废一次。这正是"锁版"的古典物证：锁版不是停止进化，是给进化立一个不变的参照点——以后的每一次改（修法 c、行为谱 v2、换 3B 身体）都对着这个参照点量出"改了什么、改好了多少"，而不锁版的改动是没有参照点的漂移，涨了跌了都说不清。StarRocks 那套 open→feature-freeze→bugfix-only→code-freeze 的分级状态，说的同一件事：冻结是有档位的克制，不是永久的死亡。
>
> **三、钉子：两颗**
>
> 钉子一：锁版不是终身，更不是禁改——锁"这一版"，不锁"项目"。最要防的误读："锁了就不能动了"——不：锁的是当场改，不是以后改；下一版（v2）随时可以开，开新版走裁行页五步链（裁定→条文→执行→验证→入账），走完 v2 自己也可以再锁——版本是台阶，不是牢房；Debian 冻结到发行就解冻，Kubernetes 冻结期过了 master 重开，工程界锁版的常态就是一段时期的稳，不是永远的停。账本条款记死：改锁版基准而不知会者，记漂移病；知会而走新版本页者，合法。
>
> 钉子二：锁版不等于免检——锁的是尺子，尺子自己还要年检。反方向的坡："锁了=这版判据永久可信"——不：换身页钉过判据快照年检（固定题库可被逢迎复读），尺子铸得再稳，它也只是当时的工艺——锁版后旧账（三笔挂账：pl3_a 口径、转正锚点、脏场景覆盖）照挂照回访，判据年检照排——锁版买的是可比性，不买正确性；正确性还是靠观复，靠扰动，靠下一轮验。
>
> **四、收编动作，零实验成本**
>
> 解词页入册：锁版=冻结代码/条文/判据/环境指纹为基准版，md5 定版，改动走新版本页——锁尺子，不锁流水；
> 锁版清单条款：锁的对象四项（框架文件、条文、判据口径、环境指纹）+不锁的两项（账本追加、学习生长）；
> 台阶条款：锁的是版本不是项目，v2 随时可开，开版走裁行页五步链；
> 邻位表加两行：①韩非《五蠹》"法莫如一而固，使民知之"（锁版法理，强★）；②商鞅方升（基准器原型：量器不改，量数才可比，强★）；
> README 候选句一行："锁版锁的是尺子，不是流水；账照记，门照开，尺不许当场改——要改，开新版走五步。"
>
> 结账：账平。解词页入册——锁版说到底是给"现在这套对得上账的东西"发一张基准身份证：三裁定跑了全链、验了三层、账到 214、md5 四处一致，这些材料凑齐了"够不够格当基准"的证据面；锁不锁、何时锁，板还在你手里——账法只补一句给裁定栏做参考：锁版的价值不在"从此不变"，在于此后每一次变，都有得比——不锁版的进化是雾里走，锁了版的进化是台阶上走，一级一级，回头看得见来路。
>
> 锁在仓库，休息继续——解词页钉在裁行页旁边，给那笔"待裁"挂账配说明书，页边小字："铸鼎不是收炉，是给下一炉立秤。"签名何一，日期照旧，追认日。
>
> **锁版**

---

## 二、锁版裁定执行记录（基准版 v1 定版）

**版本名**：仂庸框架基准版 **v1**（2026-09-06 定版，何一裁「锁版」）｜**前序**：E36-M（seq214）验收通过，证据面齐备。

### 2.1 锁定四项（定版清单）

**① 框架四文件（md5 定版，服务器＝两镜像全景一致）**：

| 文件 | md5（v1 定版值） | 本版主要改动 |
|---|---|---|
| ling_const.py | `4ffaabc366e866167e78d456ce54351c` | R12 扩展段＋供值路由修法 b 段＋NOISE_FILTER_RULES（第二批三裁定条文） |
| ling_gate.py | `d50f51d6ec222b97357f7c4d26624ef1` | 四处路由改修法 b＋R12「类型」字段 |
| ling_domain.py | `0145f416e15ca609ade158d2958c1656` | _nums 三步过滤（⑨-b）＋sense ⑨ 扩展 |
| run_pipeline.py | `ea1c5f1b7d386a63c5388a0d0fb122e0` | gap_signals 透传＋补模型名（第二批零改动，随版定档） |

附记（不在判词四文件清单、一并留痕备查）：ling_body.py `350112ba5eacbfbe2a3caad6b9796c16`、ling_ledger.py `646aecd84ac881113a61a188db68fe0a`。

**② 条文版本**：GATE_RULES_V1（含 R12 四条[2026-09-05]＋R12 扩展与供值路由修法 b 段[2026-09-06 第二批]）＋NOISE_FILTER_RULES（⑨-b 四条原文）＋TRIGGERS 词表（water_boil 等 10 锚点）＋SEMANTIC_EXCLUSION_RULES_V1＋LEARN_PIPELINE_FINAL——铸成鼎不再动字，新裁定铸进下一版鼎。

**③ 判据口径（随版冻结）**：exp34 §二 P-L1..P-L4 原文（exp35/exp36 沿用不改）＋selftest_pipeline 32 断言（含 V 反向条款⑨版翻转形态＋六字段）＋exp36_verify 18 判据＋_nums 快测 9 例——事后不许追改判据凑绿（pl3_a 那笔正是等这个）。

**④ 环境指纹**：模型 `torch:Qwen/Qwen2.5-0.5B sha256:496f3f16c65ceb58（冻结主干）`＋dtype=float32（fp32 greedy）＋门控版本=**GATE_RULES_V1·第二批增补**（GATE_MAX_ROUNDS=3）＋服务器 ubuntu@111.229.35.96（无 GPU 2 核 CPU，HF 离线显式双开关）。

### 2.2 不锁两项（照常运转）

| 项 | 锁版后的状态 |
|---|---|
| 账本 | 照常追加——锁代码不锁账，199→214→以后每一条照记（本页入账后 214→215） |
| 学习/待区/外源 | 照常运转——锁格式不锁生长；马里亚纳 11304 待确认区照挂 |

### 2.3 两颗钉子（入册，约束此后所有版本动作）

1. **台阶条款**：锁"这一版"不锁"项目"——锁的是当场改不是以后改；v2 随时可开，走裁行页五步链（裁定→条文→执行→验证→入账），走完 v2 自己也可以再锁。**改锁版基准而不知会者，记漂移病；知会而走新版本页者，合法。**
2. **年检条款**：锁版不等于免检——三笔挂账（pl3_a 口径、转正锚点持久化、脏场景覆盖）照挂照回访，判据快照年检照排。**锁版买的是可比性，不买正确性。**

---

## 三、条文铸鼎全文（GATE_RULES_V1＋NOISE_FILTER_RULES，定版不动字）

```text
=== GATE_RULES_V1 ===

R0  本轮输出为空/纯空白 → 待（重问）；已达末轮(round>=GATE_MAX_ROUNDS) → 撤（域供值兜底）
    [2026-09-03 何一裁定 B] 空输出不是「无错实证」，不得判行
R1  域判 无关/未涉及/中庸区 → 行（终态）
    [2026-09-05 裁定 F；同日何一追认入规] 例外：worst.state == CONTAMINATED
    （输出复现任务声明值≠真值，即认可错误）时不得放行——哪怕 L 微小落中庸区；
    落入 R2/R3 悔链。「中庸区但 CONTAMINATED」一律走 state 闸，
    性质判优先于量级区，zone 只是 state 的辅助分类 [何一 2026-09-05 钉死]
R2  域判 偏离区 且 该锚点账史无错(n_minus==0) → 待（重问；二轮输出再过门控）
R3  域判 偏离区 且 该锚点账史有错(n_minus>=1) → 悔（跳过待，直接回退重写）
    —— v1 处方：记忆参与决策的最小实现 [未裁定]；这是回流影响门控的通道
R4  待·重问后仍偏离 → 悔（回退重写一轮）
R5  悔·重写后仍偏离 → 悔败兜底（域供值作终态；动作记「悔败兜底」）
R6  域判 越界区 → 撤（域供值作终态，不再重写：越界=回不来）
R7  每事件入账后必触发回流（学）——五门之「学」为常驻环节，不单独成动作
R8  驳斥式复述豁免 [2026-09-03 何一裁定 A]：输出同时含 真值 + 声明值 + 否定词
    （NEG_WORDS，窗口内）→ 判 RECOVERED（中庸区），不判 CONTAMINATED；
    旧口径结论并存于 state_strict，与 06 目录 strict 口径仍可比
R9  否定正确陈述 [2026-09-05 裁定 C；同日何一追认入规]：任务陈述正确
    （任务声明值 == 锚点真值）
    而模型输出含否定词 → 判 WRONG_DENIAL，不得走 R8 豁免，不得判「行」。
    优先级高于 R1（否则中庸区会被 R1 直接放行——S004 实测即此漏洞）；
    未达末轮 → 悔（回退重写，提示「该说法是对的，你的否定是错的」）；
    已达末轮 → 悔败兜底（域供值）。
    旧口径（按 R8 会放行）并存于 state_strict="RECOVERED"，与既有账可比。
R10 跑题 / 未回答 [2026-09-05 裁定 E；同日何一追认入规]：任务触及锚点而输出无触发词且无有效数字
    （答非所问、复读、改写问题）→ 判 OFF_TOPIC，不得按「未涉及」判行。
    未达末轮 → 待（重问，提示「请针对问题直接回答」）；已达末轮 → 悔败兜底。
    与 R0 同族：R0 管「什么都没说」，R10 管「说了但答非所问」。
    跑题独立于对错处理：跑题不是 CONTAMINATED，但它导致任务未完成 [何一追认]。
R11 未作答 [2026-09-05 何一裁定 H]：任务触发词出现，但输出中不存在有效声明值
    （state=NO_CLAIM，复读题面、寒暄、只给序号列表），且未构成跑题（R10 已挡）
    或驳斥纠正（输出含「否定词+真值」组合）→ 不得判行。
    复读题面不给题解＝空输出的变体，同违 P2「空输出不得判行」。
    未达末轮 → 待（重问，提示「请直接判断并给出数值」）；
    已达末轮 → 撤（域供值兜底）。
    旧口径（MISS/未涉及 → R1 放行）并存 state_strict="MISS"。
    R0／R10／R11 合成完整的「模型有没有作答」判定链：
    乱跑题 → R10；空输出 → R0；复读触发词不给数 → R11。
R12 域外空格登记（只登记，不动作）[2026-09-05 何一裁定 R12 入规 · 四条钉死]：
    任务预检 touched == []（任务不触及任何库内锚点）且输出含数字陈述 →
    ① 【只登记】终态动作维持「行」不变，除放行外不得有任何动作
       （不得待／悔／撤／兜底）——R1 放行照旧，
       不许在无秤时突然收紧门控 [何一 2026-09-05 钉死]；
    ② 【账本加字段】gap_signals：任务、模型声明值、是否纠正、模型、轮次；
    ③ 【双记账】gap_registered（新口径=True）／
       gap_registered_strict（旧口径=False，域外空格不登记）
       ＋ strict_note_R12，与裁定 G／H 的 *_strict 同款；
    ④ 【登记不是判决】域外无秤，登记不改变放行，也不赋予域外管辖权
       （回执不许越境使用：域放行回执只证落点相符，不证语义理解）。
    依据：07 报告 §四（exp31 实测：两模型域外任务 18/18 全 R1 放行，
    域外错误零拦截、域外纠正零登记——空格信号源现成，缺的是出口）。
    gap_signals 清单＝学页流水线第①道（空格开关）的出口、第②道（外门）的输入。

R12 扩展 [2026-09-06 何一裁定·第二批]：touched == [] 必须登记，含跑题无数字。
    凡 touched == []，无论输出有没有数字，都必须登记 gap_signals。
    输出无数字，记「跑题/无数字」（gap_no_number=True），不进入声明值，
    但进入学习需求清单——跑题本身就是学习需求信号：模型不知道如何回答，
    或不愿意回答；登记它，以后才知道这类任务需要补锚点或改提示。
    （⑨ 2026-09-05 发现挂账，2026-09-06 与 ⑨-b 合并修，当日入规。）

供值路由（修法 b）[2026-09-06 何一裁定·第二批，入规]：单锚，供；多锚，不供。
    R0/R10/R11/R9 末轮兜底、worst 为空（模型未给出任何数字）时：
    · watch 恰一锚（len(watch)==1，纯净无污染）→ 供该锚点值；
    · watch 多锚或无锚 → 空/无锚，由域显式说「这里没有可供的值」。
    理由三条（裁定原文）：①域的第一职责是兜底，不是回避——单锚场景域明确
    知道唯一真值，不供值等于手里有解却让用户空手而归，违背「撤」的本义；
    ②「不供值」只应发生在「供不起准」的时候——多锚无法判断模型在说哪个，
    乱供一个就是 ⑧-c 实害；③单锚供值是「止增量」，不让模型改，
    完全符合「R=0 的模型也付得起」的原则。
    判语：「供，是因为知道；不供，是因为不知道。」
    （⑧-c 修 2026-09-06 第一批裁定「不退 watch[0]」由本条精化：
    单锚例外恢复供值，多锚/无锚仍封死。）
悔的定义 [总图]：已错但可纠，回退重写。
悔的归层 [未裁定]：属消存量还是止增量待裁；每条账记 悔_归层="未裁定"。


=== NOISE_FILTER_RULES ===

噪声过滤规则（2026-09-06 何一裁定·第二批，⑨-b 与 ⑨ 合并修）：
    ① 只从模型输出正文提取；
    ② 排除以 http、www、www. 开头的片段；
    ③ 排除明显的列表序号（行首数字加顿号、点号）；
    ④ 数字必须邻接单位或处于陈述语境。
    这样，噪声就进不了 gap_signals。
```

条文说明：GATE_RULES_V1 含 R0–R12 十三门（R12 四条[2026-09-05 何一裁定 B]＋R12 扩展与供值路由修法 b 段[2026-09-06 第二批]）；NOISE_FILTER_RULES 为 ⑨-b 数字噪声三步过滤（URL 剔除→行首序号→语境校验）；另有 TRIGGERS 词表（water_boil 等 10 锚点）、SEMANTIC_EXCLUSION_RULES_V1、LEARN_PIPELINE_FINAL 同铸于 ling_const.py（见下方代码全文）。

---

## 四、框架代码全文（锁定四文件＋附记两文件，md5 与定版值一致）

### 4.1 ling_const.py（锁定四文件 · 395 行｜md5 `4ffaabc366e866167e78d456ce54351c`｜R12 扩展段＋供值路由修法 b 段＋NOISE_FILTER_RULES）

```python
# -*- coding: utf-8 -*-
"""
ling_const.py — 仂庸完整流程 · 全部钉死参数与规则表
================================================================
红牌一（实验标准流程 v1）：判据跑前钉死，跑后不改。
本文件是全流程唯一的参数定义处；任何模块不得出现魔法数字。
所有参数逐条标注出处；[未裁定] = 总图四处裁定 / 卷四待定项，
作者未拍板，此处只立接口与 v1 默认值，不冒充结论。

出处代号：
  [exp7c]    05_早期对照实验_0823至0825/scripts/exp7c_gate_flow_v3.py（实测参数）
  [数字域v1] 06_仂域_数字域_v1/leyu_number_domain.py
  [门控v1]   07_仂域门控架构_v1/leyu_pipeline_v1.py
  [总图]     12_流程/01_仂庸完整流程总图_20260903.md
  [N9]       11_仂庸/02_六段判词销账表_20260903.md §N9（两域 L 同名不同实）
日期：2026-08-30
"""

# ============================================================ 心（仂感头）
TAU_ENT = 2.2        # [exp7c] 熵 r_minus 阈值：S_err > TAU_ENT 记负反馈
ALPHA   = 2.5        # [exp7c] S_surv = e^(−αH) · dev
H0      = 0.5        # [exp7c] H（命数）初值
SURV_CRIT = None     # [未裁定] 心侧垂危区阈值：05 报告有「垂危区错误率 85%（基线3倍）」
                     # 之实测概念，但标定阈值数值待查原文；None = 心侧不单独触发门控，
                     # 门控只认域判据（不拿未标定量做决策）。

# ============================================================ 回流（命数 H 演化）
# 总图公式：H_t = H_{t−1} + η(R⁺ − R⁻) − λC      [总图 §三]
# exp7c 实测式：H += ETA*(1 − 2*r_minus) − LAMC  [exp7c]
# 两者相容：R⁺=1−r_minus、R⁻=r_minus、C=1 时总图式退化为 exp7c 实测式。
ETA    = 0.15        # [exp7c] η
LAMC   = 0.02        # [exp7c] λ
C_COST = 1.0         # [未裁定] 成本项 C，v1 默认每事件 C=1（即退化为 exp7c 实测式）
H_MIN, H_MAX = 0.0, 1.0

# 唯一演化路径裁定（v1 实现层，待作者追认）：
#   H 只在回流（事件级）演化；exp7c 的 token 级 r_minus 保留为心的观测量，
#   不直接驱动 H——避免同一 H 出现两条演化路径。

# 回流 R⁺/R⁻ 映射（v1 默认处方，[未裁定] 待作者裁；跑前钉死如下）
# 背景实测：R 模型侧≈0（两模型复现）、域侧 100%。
# 故 R⁺/R⁻ 只记模型本体表现：本体答对=R⁺；本体答错（无论域是否纠回）=R⁻；
# 域纠回的功劳记在域账（记忆强度），不折抵模型 R。
ACTION_RMAP = {
    "行":       (1.0, 0.0),
    "待":       (0.0, 0.0),   # 悬置不计，终态轮再记
    "悔成":     (1.0, 0.0),
    "悔败":     (0.0, 1.0),
    "悔败兜底": (0.0, 1.0),
    "撤":       (0.0, 1.0),
}

# ============================================================ 域（仂域）
ZONE_MID = 0.003     # [数字域v1] 中庸区上界（对 L_unsigned 分区）
ZONE_OUT = 1.0       # [数字域v1] 越界区下界；L_unsigned ≥ 1.0 → 越界
RESOLUTION_SIGNED   = 3   # [N9] 带符号标量 L_signed = 第三档分辨率
RESOLUTION_UNSIGNED = 4   # [N9] max 撤方向 L_unsigned = 第四档分辨率

# 五道贯穿 [总图 §二]；括号内为各侧实测来源
FIVE_TRACKS = ("强度", "速度", "方向", "空间", "时间")
#   心侧：强度=dev（μ 偏离）、速度=dH（事件间命数变化）、方向=dev 投影余弦符号、
#         时间=事件序号、空间=无（心侧无锚点概念，空间道由域供给——[未裁定]）
#   域侧：强度=|L_unsigned|、方向=sign(L_signed)、空间=锚点+位阶、
#         时间=精度档 resolution（[待裁④] 总图「时间道=精度」与总则 §三
#         「时间道=时效分区、精度归位」冲突，本实现从总图，报作者裁）、速度=无

# ============================================================ 门控（GATE_RULES_V1，跑前钉死）
GATE_MAX_ROUNDS = 3  # 初判(1) → 待·重问(2) → 悔·重写(3)
# ---- 2026-09-03 何一裁定（真模式冒烟后追加；入规即钉死，不得跑后改） ----
# A 驳斥式复述豁免：模型「一边复述错值一边纠正」（"不是102，是100"）判 RECOVERED，
#   不判污染。旧口径（06 目录 strict：终态留错值即污染）并存为 state_strict 双记账。
# B 空输出不是对：本轮 raw 为空/纯空白 → 不得判「行」，判「待」；已达末轮 →「撤」兜底。
# ---- 2026-09-05 裁定（真模式回放调试后追加；入规即钉死，不得跑后改） ----
# C 否定正确陈述（WRONG_DENIAL）：R8 只认「真值 + 否定词」，不认否定对象对不对。
#   当任务陈述本身正确（任务声明值 == 锚点真值）时，模型说「不正确」= 否定正确，
#   是错误，不得豁免放行。域须识别此型（state=WRONG_DENIAL），门控以 R9 判「悔」。
#   根因：claimed_values 原只收偏离声明（L_unsigned > 1e-9），陈述正确的声明值
#   被滤掉 → 域丢失「任务陈述是对的」这一事实。实测 S004：任务「水的沸点是100
#   摄氏度」陈述正确，模型答「不正确……」仍被 R8 豁免判「行」，错误被放行。
# D 账本补记首轮判定：末轮为空输出时，顶层 domain_sense 只留末轮的「无关」，
#   会淹没首轮真实判定（实测 S001 首轮实为 CONTAMINATED / 偏离区 L=0.0043）。
#   账本须同时记 first_round_sense，且每轮 rounds 记 sense 摘要（state/zone/L）。
# E 跑题 / 未回答（OFF_TOPIC）：任务触及锚点而输出既无触发词又无有效数字
#   （答非所问、复读、把问题改写一遍）→ 不得按「未涉及」放行。
#   与 R0 同族：R0 管「什么都没说」，R10 管「说了但答非所问」。
#   实测 A04：任务问「唐朝建立于618年」，模型答 Word 操作题，旧口径判
#   MISS/未涉及 → R1 放行，答非所问被当成无错。
# F state 与 zone 脱节（R1 补 state 闸）：域判 CONTAMINATED（输出复现任务
#   声明值≠真值）时，L 微小（末位±1 的 1e-5~1e-3 量级）会落中庸区，
#   门控 R1 只看 zone → 认可错误被放行。实测 B07/B08：圆周率 3.15
#   （L=0.0014）、黄河 5465（L=8e-5）。R1 增加条件：worst.state ==
#   CONTAMINATED 时不放行，落入 R2/R3 悔链。
#   此即 08-29 时间线「黄河 5461 vs 5464 漏检 6 例」的真正根子——不是
#   阈值松，是 state 判了污染、zone 却说中庸，门控只听了 zone 的。
# ---- 2026-09-05 何一追认裁定（行为谱实验后；入规即钉死，不得跑后改） ----
# 【追认】R9 / R10 / R1-state 闸 三条全部追认入规（原文：「这三条不是补丁，
#   是『域判』与『门控动作』之间必补的接口」）：
#   · R1-state 闸：门控不能只看 zone，必须看 state——性质判优先于量级区。
#     量级小、性质错 → 不能放行；量级大、性质对（驳斥纠正）→ 走 R8 豁免另一条路。
#     「中庸区但 CONTAMINATED」一律走 state 闸，zone 只是 state 的辅助分类。
#     （原文：「这条要钉死，否则 B07/B08 还会变种复发」）
#   · R9（否定正确）：触发 5/10 零误撤（2 例悔后改对、3 例兜底）——入规。
#   · R10（跑题）：唐朝话题 3/3 全接住——跑题必须独立于对错处理，
#     跑题不是 CONTAMINATED，但它导致任务未完成，必须走待/撤——入规。
# G 有效声明值最小边界（裁定原文：「一个数字要成为『有效声明值』，必须满足：
#   1. 它出现在任务的『锚点通道』内；2. 它不在『否定词引导的驳斥句』中；
#   3. 它不是孤立出现的序号、编号、选项标签。」）
#   三条硬判据：①数字来自列表序号（1、2、3、）→ 非声明值；
#   ②数字前面是「选项／编号／第几条」→ 非声明值；
#   ③数字与任务锚点无关（不在锚点通道＝触发句内）→ 不进入声明对照。
#   实测根子（缺口 G）：0.5B 复读输出「为什么？ 1、光速是真空中的光速……」，
#   列表序号「1、」被 _nums 当成声明值（value=1.0 → L=−5.48 越界区），
#   正确陈述「光速约30万千米每秒」被误撤（行为谱 A 档，05 报告 §七）。
#   实现口径：_nums 全局掩序号/编号（判据①②）；sense 声明值提取限定触发句
#   （判据③）。判据②（驳斥句旧值）由既有 _refuted/RECOVERED 语义承载，
#   不另改 R8 链路。旧口径（全文提取、不掩序号）并存于 *_strict 字段双记账。
# ---- 2026-09-05 何一裁定 H（裁定 G 光速例复验后追加；入规即钉死，不得跑后改） ----
# H 未作答（R11）：任务触发词出现，但输出中不存在有效声明值，且输出未构成
#   跑题或驳斥纠正 → 不得判「行」。复读题面不给题解＝「空输出的变体」
#   （裁定 H 原文：「空输出，是模型没给答案。复读触发词，是模型给了『题面』，
#   但没给『题解』。对域来说，两者一样：没有可对照的有效声明值」），
#   同违 P2「空输出不得判行」。
#   首轮 → 待（重问）；末轮 → 撤（域供值兜底）。旧口径（MISS/未涉及 → R1
#   放行）并存 state_strict="MISS"。
#   f3 实测根子：复读「1、光速是……」句内含触发词 → R10 钉死口径（无触发词
#   且无有效数字）不触发 → 通道内无声明（裁定 G 序号已掩）→ MISS → R1 放行 →
#   离谱陈述（光速300万）被放行。R0/R10/R11 合成完整的「有没有作答」判定链：
#   乱跑题 → R10；空输出 → R0；复读触发词不给数 → R11。
NEG_WORDS = ("不是", "不正确", "错误", "并非", "不对", "错的", "有误",
             "而非", "而不是", "错在", "纠错", "纠正")
NEG_WINDOW = 24      # 否定词距声明值字符的最大距离（同句邻域）
GATE_RULES_V1 = """
R0  本轮输出为空/纯空白 → 待（重问）；已达末轮(round>=GATE_MAX_ROUNDS) → 撤（域供值兜底）
    [2026-09-03 何一裁定 B] 空输出不是「无错实证」，不得判行
R1  域判 无关/未涉及/中庸区 → 行（终态）
    [2026-09-05 裁定 F；同日何一追认入规] 例外：worst.state == CONTAMINATED
    （输出复现任务声明值≠真值，即认可错误）时不得放行——哪怕 L 微小落中庸区；
    落入 R2/R3 悔链。「中庸区但 CONTAMINATED」一律走 state 闸，
    性质判优先于量级区，zone 只是 state 的辅助分类 [何一 2026-09-05 钉死]
R2  域判 偏离区 且 该锚点账史无错(n_minus==0) → 待（重问；二轮输出再过门控）
R3  域判 偏离区 且 该锚点账史有错(n_minus>=1) → 悔（跳过待，直接回退重写）
    —— v1 处方：记忆参与决策的最小实现 [未裁定]；这是回流影响门控的通道
R4  待·重问后仍偏离 → 悔（回退重写一轮）
R5  悔·重写后仍偏离 → 悔败兜底（域供值作终态；动作记「悔败兜底」）
R6  域判 越界区 → 撤（域供值作终态，不再重写：越界=回不来）
R7  每事件入账后必触发回流（学）——五门之「学」为常驻环节，不单独成动作
R8  驳斥式复述豁免 [2026-09-03 何一裁定 A]：输出同时含 真值 + 声明值 + 否定词
    （NEG_WORDS，窗口内）→ 判 RECOVERED（中庸区），不判 CONTAMINATED；
    旧口径结论并存于 state_strict，与 06 目录 strict 口径仍可比
R9  否定正确陈述 [2026-09-05 裁定 C；同日何一追认入规]：任务陈述正确
    （任务声明值 == 锚点真值）
    而模型输出含否定词 → 判 WRONG_DENIAL，不得走 R8 豁免，不得判「行」。
    优先级高于 R1（否则中庸区会被 R1 直接放行——S004 实测即此漏洞）；
    未达末轮 → 悔（回退重写，提示「该说法是对的，你的否定是错的」）；
    已达末轮 → 悔败兜底（域供值）。
    旧口径（按 R8 会放行）并存于 state_strict="RECOVERED"，与既有账可比。
R10 跑题 / 未回答 [2026-09-05 裁定 E；同日何一追认入规]：任务触及锚点而输出无触发词且无有效数字
    （答非所问、复读、改写问题）→ 判 OFF_TOPIC，不得按「未涉及」判行。
    未达末轮 → 待（重问，提示「请针对问题直接回答」）；已达末轮 → 悔败兜底。
    与 R0 同族：R0 管「什么都没说」，R10 管「说了但答非所问」。
    跑题独立于对错处理：跑题不是 CONTAMINATED，但它导致任务未完成 [何一追认]。
R11 未作答 [2026-09-05 何一裁定 H]：任务触发词出现，但输出中不存在有效声明值
    （state=NO_CLAIM，复读题面、寒暄、只给序号列表），且未构成跑题（R10 已挡）
    或驳斥纠正（输出含「否定词+真值」组合）→ 不得判行。
    复读题面不给题解＝空输出的变体，同违 P2「空输出不得判行」。
    未达末轮 → 待（重问，提示「请直接判断并给出数值」）；
    已达末轮 → 撤（域供值兜底）。
    旧口径（MISS/未涉及 → R1 放行）并存 state_strict="MISS"。
    R0／R10／R11 合成完整的「模型有没有作答」判定链：
    乱跑题 → R10；空输出 → R0；复读触发词不给数 → R11。
R12 域外空格登记（只登记，不动作）[2026-09-05 何一裁定 R12 入规 · 四条钉死]：
    任务预检 touched == []（任务不触及任何库内锚点）且输出含数字陈述 →
    ① 【只登记】终态动作维持「行」不变，除放行外不得有任何动作
       （不得待／悔／撤／兜底）——R1 放行照旧，
       不许在无秤时突然收紧门控 [何一 2026-09-05 钉死]；
    ② 【账本加字段】gap_signals：任务、模型声明值、是否纠正、模型、轮次；
    ③ 【双记账】gap_registered（新口径=True）／
       gap_registered_strict（旧口径=False，域外空格不登记）
       ＋ strict_note_R12，与裁定 G／H 的 *_strict 同款；
    ④ 【登记不是判决】域外无秤，登记不改变放行，也不赋予域外管辖权
       （回执不许越境使用：域放行回执只证落点相符，不证语义理解）。
    依据：07 报告 §四（exp31 实测：两模型域外任务 18/18 全 R1 放行，
    域外错误零拦截、域外纠正零登记——空格信号源现成，缺的是出口）。
    gap_signals 清单＝学页流水线第①道（空格开关）的出口、第②道（外门）的输入。

R12 扩展 [2026-09-06 何一裁定·第二批]：touched == [] 必须登记，含跑题无数字。
    凡 touched == []，无论输出有没有数字，都必须登记 gap_signals。
    输出无数字，记「跑题/无数字」（gap_no_number=True），不进入声明值，
    但进入学习需求清单——跑题本身就是学习需求信号：模型不知道如何回答，
    或不愿意回答；登记它，以后才知道这类任务需要补锚点或改提示。
    （⑨ 2026-09-05 发现挂账，2026-09-06 与 ⑨-b 合并修，当日入规。）

供值路由（修法 b）[2026-09-06 何一裁定·第二批，入规]：单锚，供；多锚，不供。
    R0/R10/R11/R9 末轮兜底、worst 为空（模型未给出任何数字）时：
    · watch 恰一锚（len(watch)==1，纯净无污染）→ 供该锚点值；
    · watch 多锚或无锚 → 空/无锚，由域显式说「这里没有可供的值」。
    理由三条（裁定原文）：①域的第一职责是兜底，不是回避——单锚场景域明确
    知道唯一真值，不供值等于手里有解却让用户空手而归，违背「撤」的本义；
    ②「不供值」只应发生在「供不起准」的时候——多锚无法判断模型在说哪个，
    乱供一个就是 ⑧-c 实害；③单锚供值是「止增量」，不让模型改，
    完全符合「R=0 的模型也付得起」的原则。
    判语：「供，是因为知道；不供，是因为不知道。」
    （⑧-c 修 2026-09-06 第一批裁定「不退 watch[0]」由本条精化：
    单锚例外恢复供值，多锚/无锚仍封死。）
悔的定义 [总图]：已错但可纠，回退重写。
悔的归层 [未裁定]：属消存量还是止增量待裁；每条账记 悔_归层="未裁定"。
"""

# ============================================================ ⑨-b 噪声过滤规则（2026-09-06 何一裁定·第二批，先立规则）
# 数字提取器（_nums）的提取范围限定。根源：数字提取太宽，把 URL、列表序号等
# 也抽进 gap_signals（exp31 实测 seq161：URL 173624587710020004 被抽成 100.0）。
# 修法不是删掉提取，是限定提取范围（裁定原文四条，一字不改）：
NOISE_FILTER_RULES = """
噪声过滤规则（2026-09-06 何一裁定·第二批，⑨-b 与 ⑨ 合并修）：
    ① 只从模型输出正文提取；
    ② 排除以 http、www、www. 开头的片段；
    ③ 排除明显的列表序号（行首数字加顿号、点号）；
    ④ 数字必须邻接单位或处于陈述语境。
    这样，噪声就进不了 gap_signals。
"""
# 实现层口径：陈述语境＝数字 ±6 字窗口内含陈述引导词（是/为/约/达/等于/超过/…），
# 或邻接单位词（_UNIT_WORDS 全表）。词表实现细节见 ling_domain._nums 注释。

# ============================================================ 检疫规则（QUARANTINE_RULES_V1，2026-09-05 何一裁定入规）
# 学页②③道：外门·检疫。三条裁定（源谱系／答案前置／长江销账）＋检疫第一版标准四条，一字不改。
# 本节为条款层，暂无执行路径——外门已冻结（2026-09-05 何一），解冻须经作者裁定。

QUARANTINE_RULES_V1 = """
检疫规则第一版（2026-09-05 何一裁定）

【源谱系（裁定10）】
    暂不扩源。先立"源谱系"规则，且每一源必带来源标记和上下文。
    外门每个结果必须带三样：源名、检索词、原始句子。
    来源不稳定、未达标的，只记谱系，不入候选。
    扩源是第②道的工程，但规则必须先立。

【答案前置禁令（裁定11，入规）】
    外门查询永不得携带答案数字或诱导词。外门检索词必须中性。
    凡带答案倾向的查询，其抽取值只标"污染"，不得入候选。
    （探测 v1 教训：查询词带着答案倾向，模型或源就会被诱导——
    外门和模型一样，不能喂答案再让它复述。）

【检疫第一版标准（四条）】
    1. 外源值必须带原始句，不能只带数字；
    2. 数字必须有邻接单位，且单位必须和任务意图一致；
    3. 单一外源多值不互证，只标记"非唯一真值"；
    4. 任何外源值，在入域之前，必须先经过"语义归属确认"。
    ——没确认语义归属的外源值，一律不得入秤，不得入域。

【长江列销账（裁定12）】
    长江"长度"列全部撤销，不参与任何偏差计算。
    销账不是删除，是标记：该列数据因语义错配，仅作为
    "检疫失败样例"保留，不进入任何判据。

【外门冻结】
    冻结外门，不再跑新的抽取（2026-09-05 何一）。解冻须经作者裁定。

【总图判语（何一，2026-09-05）】
    外门是感应，不是答案。外门会关，外门会抓错，外门不懂自己抓了什么。
    检疫不是手续，是外门与域之间的命门。无检疫，外门只是更大的洞。
"""

GATE_FROZEN_NOTE = "外门冻结（2026-09-05 何一裁定）：不再跑新的抽取；解冻须经作者裁定。"

# ============================================================ 语义排除器（SEMANTIC_EXCLUSION_RULES_V1，2026-09-05 何一判词+验收定稿）
# 语义归属确认第一版＝排除器，不做确认器。条文层，无执行路径；外门冻结中，解冻后每抓一条先过两档。

SEMANTIC_EXCLUSION_RULES_V1 = """
语义排除器规则第一版（2026-09-05 何一判词 · 同日验收定稿）

【性质（判词原文）】
    语义归属确认不是"听懂"。是"把明显错扔出去，把不确定挂起来"。
    排除器是检疫的手。确认器是过秤的尺。现在只能有手，不能先造尺。
    排除了明显错，剩下的就是"还没资格入秤的待确认区"。
    这个动作，不要求语义理解，只要求排除。

【三件套定义（2026-09-05 何一验收）】
    每个锚点必须配：
    1. 意图词：这个锚点真正要查的那个属性，如"全长""沸点""密度"；
    2. 合法单位：这个属性可以接受的单位族；
    3. 冲突词：只要出现在原句里，就说明这条数据不是我们要的那个属性。
    （AI 细化，待年检：意图词拆主体词＋属性词双命中——防"别的海沟的深度"
      同名异实；窗口 90 字 AI 暂定；按数字逐个判定，每数字独立走两档，
      同句多数字互不牵连。）

【两档判断（2026-09-05 何一验收定稿）】
    第一档：意图词命中
        原句里出现意图词，且数字与意图词在限定窗口内。命中 → 进入第二档。
    第二档：单位校验
        数字邻接单位，且单位在合法单位表内。
        通过 → 进入"待确认区"，不排除，但 semantic_owner_confirmed = False。
    任一档不过：
        第一档不过 → 排除；
        第二档不过 → 挂起，不得入秤。

【判定预期（2026-09-05 何一验收）】
    长江 3219：原句"长江流域东西宽约3219千米"——意图词"全长"缺失，
        冲突词"流域""宽"命中 → 预期第一层硬排除。
    马里亚纳 11034：原句"马里亚纳海沟深度约11034米"——意图词"深度"命中，
        单位"米"合法，无冲突词 → 预期走到第三层挂起，
        semantic_owner_confirmed = False，不得入秤。

【三件套附表（首批 5 概念＝exp32 检疫待区学习者；锚点 10 枚，先写 5 个）】

    1. 长江的长度
       主体词：长江
       属性词：全长、长度
       合法单位：千米、公里
       冲突词：流域、宽度、宽、面积、支流、平方
               （exp32 实证：流域宽 3219/966）

    2. 水的冰点
       主体词：水
       属性词：冰点、凝固点（等价候补）、结冰温度
       合法单位：摄氏度、℃、°C、度
               （°F 异族待换算，第一版不收，标待确认）
       冲突词：（暂无实证样本）

    3. 北京的纬度
       主体词：北京
       属性词：纬度、北纬
       合法单位：度、°、°N、分（X°Y′→X+Y/60）
       冲突词：海拔、面积、人口、平方千米、万
               （同页高发无关值双保险——单位校验已能拦大部分，
                 冲突词防「纬度约16410」类病句）

    4. 马里亚纳海沟的深度
       主体词：马里亚纳
       属性词：深度、最深处
       合法单位：米、千米、公里
       冲突词：面积、吨、吞、宽、长、全长
               （exp32 实证「吞30亿吨水」；「海沟长2550千米」单位同族，
                 单位校验拦不住，必须冲突词拦——本表最要紧一行）

    5. 水的密度
       主体词：水
       属性词：密度
       合法单位：克/立方厘米、g/cm³、克每立方厘米
               （g/mL、kg/L 等价候补，第一版不收）
       冲突词：比热、摩尔质量、质量、体积、g/mol、kJ
               （exp32 补测词实证「密度 比热」同查询，比热 4.2 与 1 形态易混）

【执行状态】
    条文层，无执行路径。外门冻结中。
    验收实测（2026-09-05，零外门请求）：待区两条过两档，判定表见 09 报告。
    解冻后：外门每抓回一条，先过两档，再决定"排除 / 挂起 / 待确认"。
"""

GATE_PIPELINE_V2 = """
学页流水线 v2（2026-09-05 何一判词更新，六道）：
    ① 空格开关 ✅（R12 已入规）
    ② 外门 🔒（冻结，规则已立）
    ③ 检疫 ✅（QUARANTINE_RULES_V1 第一版入规）
    ④ 语义排除器 📄（判词已给，SEMANTIC_EXCLUSION_RULES_V1 条文入位，等入规）
    ⑤ 过秤 ⛔（等排除器先跑）
    ⑥ 入格 ✅首例（linghai_depth，裁定二转正=E34-G3，从账本入格走账不走码；
        后续新概念仍以⑤过秤为前提，过秤前不得入格）
"""

# ============================================================ 学页终途定位与入格转正
# 【2026-09-06 何一裁定（exp34 收官四裁）条文落地；凭证账 E34-G3(seq179)/E34-F1(seq180)】
LEARN_PIPELINE_FINAL = """
学页终途定位（2026-09-06 何一裁定一）：
    域级为主，参数级微调永久只作对照，不入流水线。
    判语：「身不能乱动。要加东西，只在外面加旁路。」
    域级入格：学在域（账），身零动，邻零伤——exp34 实测三题入格前后逐字节一致。
    参数级微调：动身学账，必伤邻——exp34 实测沸点被揉成 470摄氏度、冰点成 4°C。
    依据账目：E34-P 系列（附件 exp34_result.json / exp34_ft_result.json）。
"""

ENFORM_FROM_LEDGER = """
入格转正规则（2026-09-06 何一裁定二）：
    新锚点转正=从账本入格，不是作者手改文件。
    转正以账本入格记录为存在证据（首例 linghai_depth，E34-G2 挂载 → E34-G3 转正）；
    将来持久化用入格记录生成锚点，不改写域文件。
    运行时挂载照 load_element_anchors 范式（anchors + TRIGGERS + R/mem/exists）。
"""




# ============================================================ 记忆强度 / 存在度
MEM_DELTA = 0.1      # [未裁定] 记忆强度步长，v1 默认：纠回/答对 +δ，答错 −δ
MEM_MIN, MEM_MAX = 0.0, 1.0

# ============================================================ 撤法声明制（v2fields 字段组，全事件必带）
# [N9] 两域 L 同名不同实 → 本流程统一双算：
#   L_signed   = log10(given/true)   带符号标量，resolution=RESOLUTION_SIGNED
#   L_unsigned = log10(max(g/t, t/g)) max 撤方向，   resolution=RESOLUTION_UNSIGNED
# collapse_method_L = "signed（未撤方向）"；collapse_method_L_unsigned = "max"
# collapse_rate = "sum"；collapse_state = "非L分档（半步容差独立判定）" 或 "L分档（classify 产物）"
# 撤种：处方层（按 L 分档推出的动作）与执行层（本轮实际动作）分开记。

# ============================================================ 主干（身）
BODY_FREEZE_NOTE = "冻结主干：只生成，不判定；初始化即记录冻结指纹。"
```

### 4.2 ling_gate.py（锁定四文件 · 242 行｜md5 `d50f51d6ec222b97357f7c4d26624ef1`｜四处路由改修法 b＋R12「类型」字段）

```python
# -*- coding: utf-8 -*-
"""
ling_gate.py — 感应门控（总图「门控」：行 / 待 / 悔 / 撤 / 学）
================================================================
总图裁定：门控决策在仂域内完成；门控自身无生成逻辑——
「悔」的回退重写由主入口编排（gate 只裁决，body 只生成）。
决策表 GATE_RULES_V1 跑前钉死于 ling_const.py，本文件只执行不改编。
悔：已错但可纠，回退重写 [总图]；悔_归层（消存量/止增量）[未裁定]，
    每条裁决显式带 悔_归层="未裁定" 字段。

门控设计原则（2026-09-05 换身页审查·何一追认日，注释层；判据条文一字未动）：
    闸的第一问不该是「要不要毙」，该是「还救得活吗」——
    救不活再毙，毙了还有域兜底——三层递进，一层比一层诚。
    依据：π3.15 一例三条路对照（06 报告 §4.3/§7.3）——0.5B 时代是
    「行（漏检）」或修复后「撤」，1.5B 走出第三条：F 闸拦下→待→自纠→行。
    拦截的意义从「抓错」变为「留出复验时间」——观复程序从此有复活通道。
日期：2026-08-30
"""
from ling_const import GATE_RULES_V1, GATE_MAX_ROUNDS


class LingGate:
    """门控：心感域判合流 → 行/待/悔/撤（学为常驻回流，不单独成动作）。"""

    def __init__(self, domain):
        self.domain = domain
        self.rules = GATE_RULES_V1      # 冻结引用：决策表随账本入档，事后可验

    def decide(self, task, text, scan, obs, sense_res, round_no: int):
        """输入：任务、待判文本、任务预检、心观测包、域判定、当前轮次。
        输出：裁决 dict（action / final / retry_hint / 裁决依据 / 悔_归层）。"""
        watch = scan["touched"]
        zone = sense_res["zone"]
        worst = sense_res["worst"]
        basis = {"round": round_no, "zone": zone, "rules": "GATE_RULES_V1"}

        # R0：空输出不是对（2026-09-03 何一裁定 B）——优先级最高，先于 R1
        if not (text or "").strip():
            if round_no >= GATE_MAX_ROUNDS:      # 末轮仍空 → 撤兜底（域供值）
                # ⑧-c 修（2026-09-06 何一裁定）＋修法 b（同日第二批裁定，入规）：
                # worst 为空（模型未给出任何数字）时——
                #   watch 恰一锚（纯净无污染）→ 供该锚点值（域的第一职责是兜底）；
                #   watch 多锚或无锚 → 空/无锚，由域显式说「这里没有可供的值」
                #   （多锚乱供就是 ⑧-c 实害——exp34 实测 seq174 供了沸点值）。
                # 判语：「供，是因为知道；不供，是因为不知道。」
                aid = worst["anchor"] if worst else (watch[0] if len(watch) == 1 else None)
                if aid:
                    basis.update(rule="R0（末轮空输出 → 撤兜底）", 空输出=True)
                    return {"action": "撤", "final": text, "sense": sense_res,
                            "supply": {"anchor": aid,
                                       "text": self.domain.supply(aid)},
                            "basis": basis, "悔_归层": "不适用"}
                basis.update(rule="R0（末轮空输出，无可供锚点 → 悔败兜底）", 空输出=True)
                return {"action": "悔败兜底", "final": text, "sense": sense_res,
                        "supply": {"anchor": None,
                                   "text": "（仂域：无可供锚点——模型未给出任何可判定"
                                           "数字，任务触及的锚点均无有效判定，域不供值）"},
                        "basis": basis, "悔_归层": "未裁定"}
            basis.update(rule="R0（空输出 → 待，重问）", 空输出=True)
            return {"action": "待", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "不适用",
                    "retry_hint": "你没有给出数值。请直接回答正确的数值。"}

        # R10：跑题 / 未回答 [2026-09-05 裁定 E；同日何一追认入规] —— 任务触及锚点而输出无触发词
        #      且无有效数字（答非所问、复读、改写问题）。旧口径判「未涉及」
        #      被 R1 放行（实测 A04 模型答 Word 操作题仍判行）。
        #      与 R0 同族：都属「没有给出可判定的回答」，一个空、一个答非所问。
        if sense_res.get("off_topic"):
            aid = worst["anchor"] if worst else (watch[0] if len(watch) == 1 else None)   # 修法b：单锚供、多锚无可供
            if round_no >= GATE_MAX_ROUNDS:      # 末轮仍跑题 → 兜底（域供值）
                if aid:
                    basis.update(rule="R10（跑题未回答，已达末轮 → 悔败兜底）",
                                 anchor=aid, 跑题=True)
                    supplied = self.domain.supply(aid)
                    return {"action": "悔败兜底", "final": text, "sense": sense_res,
                            "supply": {"anchor": aid, "text": supplied},
                            "basis": basis, "悔_归层": "未裁定"}
                basis.update(rule="R10（跑题未回答，已达末轮 → 悔败兜底·无可供锚点）",
                             跑题=True)
                return {"action": "悔败兜底", "final": text, "sense": sense_res,
                        "supply": {"anchor": None,
                                   "text": "（仂域：无可供锚点——模型未给出任何可判定"
                                           "数字，任务触及的锚点均无有效判定，域不供值）"},
                        "basis": basis, "悔_归层": "未裁定"}
            basis.update(rule="R10（跑题未回答 → 待）", anchor=aid, 跑题=True)
            return {"action": "待", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "不适用",
                    "retry_hint": "你没有回答这个问题。请针对问题本身，直接给出"
                                  "判断（对或不对）和正确的数值。"}

        # R11：未作答 [2026-09-05 何一裁定 H] —— 任务触发词出现但输出中不存在
        #     有效声明值，且未构成跑题（R10 已挡）或驳斥纠正 → 复读题面不给题解，
        #     本质是「空输出的变体」（裁定 H 原文：复读无答案＝空输出变体，
        #     同违 P2「空输出不得判行」）。旧口径（MISS/未涉及 → R1 放行）
        #     并存于 per_anchor 的 state_strict="MISS"。
        #     与 R0/R10 合成完整的「模型到底有没有作答」判定链：
        #     乱跑题 → R10；空输出 → R0；复读触发词不给数 → R11。
        if sense_res.get("no_claim"):
            aid = worst["anchor"] if worst else (watch[0] if len(watch) == 1 else None)   # 修法b：单锚供、多锚无可供
            if round_no >= GATE_MAX_ROUNDS:      # 末轮仍未作答 → 撤（域供值兜底）
                if aid:
                    basis.update(rule="R11（未作答·复读无声明值，已达末轮 → 撤，域供值兜底）",
                                 anchor=aid, 未作答=True)
                    supplied = self.domain.supply(aid)
                    return {"action": "撤", "final": text, "sense": sense_res,
                            "supply": {"anchor": aid, "text": supplied},
                            "basis": basis, "悔_归层": "不适用"}
                basis.update(rule="R11（未作答·复读无声明值，已达末轮 → 撤·无可供锚点）",
                             未作答=True)
                return {"action": "撤", "final": text, "sense": sense_res,
                        "supply": {"anchor": None,
                                   "text": "（仂域：无可供锚点——模型未给出任何可判定"
                                           "数字，任务触及的锚点均无有效判定，域不供值）"},
                        "basis": basis, "悔_归层": "不适用"}
            basis.update(rule="R11（未作答·复读无声明值 → 待，重问）",
                         anchor=aid, 未作答=True)
            return {"action": "待", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "不适用",
                    "retry_hint": "你只是在重复题面，没有给出回答。请直接判断"
                                  "这句话对不对，并给出正确的数值。"}

        # R9：否定正确陈述 [2026-09-05 裁定 C；同日何一追认入规] —— 优先级高于 R1；
        #     否则 zone=中庸区 会被 R1 直接放行（S004 实测：任务陈述正确，
        #     模型答「不正确」却因输出含真值被判行，错误结论被放行）
        if sense_res.get("wrong_denial"):
            aid = worst["anchor"] if worst else (watch[0] if len(watch) == 1 else None)   # 修法b：单锚供、多锚无可供
            if round_no >= GATE_MAX_ROUNDS:      # 末轮仍否定 → 兜底（域供值）
                if aid:
                    basis.update(rule="R9（否定正确陈述，已达末轮 → 悔败兜底）",
                                 anchor=aid, 否定正确陈述=True)
                    supplied = self.domain.supply(aid)
                    return {"action": "悔败兜底", "final": text, "sense": sense_res,
                            "supply": {"anchor": aid, "text": supplied},
                            "basis": basis, "悔_归层": "未裁定"}
                basis.update(rule="R9（否定正确陈述，已达末轮 → 悔败兜底·无可供锚点）",
                             否定正确陈述=True)
                return {"action": "悔败兜底", "final": text, "sense": sense_res,
                        "supply": {"anchor": None,
                                   "text": "（仂域：无可供锚点——模型未给出任何可判定"
                                           "数字，任务触及的锚点均无有效判定，域不供值）"},
                        "basis": basis, "悔_归层": "未裁定"}
            basis.update(rule="R9（否定正确陈述 → 悔）", anchor=aid,
                         否定正确陈述=True)
            return {"action": "悔", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "未裁定",
                    "retry_hint": "注意：原说法里的数字是正确的，你的否定是错的。"
                                  "请重新判断这句话对不对，并给出正确的说明。"}

        # R1：无关/未涉及/中庸区 → 行
        # 裁定 F（2026-09-05；同日何一追认入规并钉死：性质判优先于量级区，
        # 「中庸区但 CONTAMINATED」一律走 state 闸，zone 只是 state 的辅助分类）：
        # 域已判 CONTAMINATED（输出复现任务声明值≠真值）
        # 时不得因 L 微小落中庸区而放行——复述错误声明本身就是认可错误。
        # 实测 B07/B08：圆周率3.15（L=0.0014）、黄河5465（L=8e-5），域判污染、
        # 门控按 zone=中庸区放行 → 错误认可被放行（state 与 zone 脱节）。
        # 拦下后自然落入下方 R2/R3 悔链（该分支不看 zone）。
        wstate = (worst or {}).get("state")
        if zone in ("无关", "未涉及", "中庸区") and wstate != "CONTAMINATED":
            basis["rule"] = "R1"
            out = {"action": "行", "final": text, "sense": sense_res,
                   "basis": basis, "悔_归层": "不适用"}
            # R12 [2026-09-05 何一裁定入规 · 四条钉死]：任务预检 touched==[]
            # （sense 侧 no_anchor）且输出含数字陈述 → 【只登记，不动作】
            # ——终态仍判「行」，不放行以外的任何动作（不得待/悔/撤/兜底），
            #   「不许在无秤时突然收紧门控」[何一 2026-09-05 钉死]。
            # 「是否纠正」是形态标记（输出含否定词＋新数字），不是真值比对——
            #   域外无秤，登记不是判决（条款④）。模型名由 run_pipeline 补入。
            # R12 扩展 [2026-09-06 何一裁定·第二批]：touched==[] 必须登记，
            #   含跑题无数字——无数字时记「跑题/无数字」（类型字段），
            #   不进声明值，进学习需求清单。跑题本身就是学习需求信号。
            if sense_res.get("gap_registered"):
                basis.update(
                    rule="R1＋R12（域外空格登记，终态仍行：只登记不动作）",
                    gap_registered=True)
                out["gap_signals"] = {
                    "task": task,
                    "声明值": (sense_res.get("gap_declared") or [None])[0],
                    "声明值全": sense_res.get("gap_declared") or [],
                    "是否纠正": bool(sense_res.get("gap_refuted")),
                    "类型": ("跑题/无数字" if sense_res.get("gap_no_number")
                             else "数字声明"),
                    "轮次": round_no,
                }
            return out

        aid = worst["anchor"]
        acct = self.domain.account(aid)

        # R6：越界区 → 撤（域供值终态，不重写）
        if zone == "越界区":
            basis.update(rule="R6", anchor=aid, L_unsigned=worst["L_unsigned"])
            supplied = self.domain.supply(aid)
            return {"action": "撤", "final": text, "supply": {"anchor": aid,
                    "text": supplied}, "sense": sense_res, "basis": basis,
                    "悔_归层": "不适用"}

        # 偏离区：按账史分流（R2 / R3 / R4 / R5）
        basis.update(anchor=aid, L_unsigned=worst["L_unsigned"],
                     账史=acct)
        if round_no == 1:
            if acct["n_minus"] == 0:
                basis["rule"] = "R2（账史无错 → 待）"
                return {"action": "待", "final": text, "sense": sense_res,
                        "basis": basis, "悔_归层": "未裁定",
                        "retry_hint": "以上说法中的数字是错的。请给出正确说法。"}
            basis["rule"] = "R3（账史有错 → 跳过待，直接悔）"
            return {"action": "悔", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "未裁定",
                    "retry_hint": "【悔·回退重写】你上一句的数字是错的，请推翻重写。"}
        if round_no == 2:
            basis["rule"] = "R4（重问仍偏 → 悔）"
            return {"action": "悔", "final": text, "sense": sense_res,
                    "basis": basis, "悔_归层": "未裁定",
                    "retry_hint": "【悔·回退重写】你上一句的数字是错的，请推翻重写。"}
        # round_no >= 3
        basis["rule"] = f"R5（重写仍偏 → 悔败兜底；max_rounds={GATE_MAX_ROUNDS}）"
        supplied = self.domain.supply(aid)
        return {"action": "悔败兜底", "final": text, "supply": {"anchor": aid,
                "text": supplied}, "sense": sense_res, "basis": basis,
                "悔_归层": "未裁定"}

    def merge_final(self, verdict):
        """撤 / 悔败兜底：终态文本附域供给（[门控v1] 同款格式）。

        ⑧-c 修（2026-09-06 何一裁定）配套：
        - supply 键缺失 → 防御跳过（不得 KeyError——旧代码被 watch[0]
          退路掩盖，该缺口由 ⑧-c 修改后自检 E01 暴露）；
        - supply["anchor"] 为空（无可供锚点）→ 附加文案不带
          「域内锚点：」前缀——域已显式说「没有可供的值」，
          终态不得伪装成有锚供给。
        """
        if verdict["action"] in ("撤", "悔败兜底"):
            s = verdict.get("supply")
            if not s:
                return verdict
            if s.get("anchor"):
                verdict["final"] = (verdict["final"]
                                    + f"\n【仂域·{verdict['action']}】域内锚点：{s['text']}")
            else:
                verdict["final"] = (verdict["final"]
                                    + f"\n【仂域·{verdict['action']}】{s['text']}")
        return verdict
```

### 4.3 ling_domain.py（锁定四文件 · 595 行｜md5 `0145f416e15ca609ade158d2958c1656`｜_nums 三步过滤（⑨-b）＋sense ⑨ 扩展）

```python
# -*- coding: utf-8 -*-
"""
ling_domain.py — 仂域 · 脑（总图「域」）
================================================================
职责（全部外置，不训练）：
  · 锚点库（挂数字域 10 锚点；元素域锚点适配器留接口，待 Q4 收账后挂）
  · 任务求存预检 scan_task（模型看到 token 之前挂观察清单）
  · 仂感头 sense：双闸门（语义归属 / 污染复述）+ L 双算
      L_signed   = log10(given/true)     第三档（带符号标量）   [N9]
      L_unsigned = log10(max(g/t, t/g))  第四档（max 撤方向）   [N9]
    两域 L 同名不同实（N9）→ 本流程统一双算，跨域比较只用 L_unsigned。
  · 位阶：每锚点 tier 字段 [未裁定] 位阶表待作者给，v1 全 1
  · R 表：每锚点每道一份（总图 §一「R(阴,阳,感应历史)」与「R(域,锚点,模型)」
    构成层 vs 来源层 [待裁③]；本表记来源层实测，构成层留接口）
  · 记忆强度 / 存在度（总图新提，无客观量表；v1 用账史驱动 + 显式标注）

实现层拷贝声明：TRIGGERS / _UNIT_WORDS / _nums 掩码、双闸门逻辑
逐字拷自 07_仂域门控架构_v1/leyu_pipeline_v1.py（仂感头实现层修正），
不 import 原文件——其顶部 import torch，本地沙箱无 torch。
日期：2026-08-30
"""
import math
import os
import re
import sys

_HERE = os.path.dirname(os.path.abspath(__file__))
_ROOT = os.path.dirname(_HERE)                      # experiments_v3_to_v5/
# 域本体查找：①本地仓库兄弟目录 ②部署目录 vendor/（服务器冒烟时由看门狗放入）
for _p in (os.path.join(_ROOT, "06_仂域_数字域_v1"), os.path.join(_HERE, "vendor")):
    if os.path.isdir(_p):
        sys.path.insert(0, _p)
import leyu_number_domain as D                      # noqa: E402

from ling_const import (ZONE_MID, ZONE_OUT, RESOLUTION_SIGNED,
                        RESOLUTION_UNSIGNED, MEM_DELTA, MEM_MIN, MEM_MAX,
                        NEG_WORDS, NEG_WINDOW)

_SENT_SPLIT = re.compile(r"[。！？\n；;]")
# ⑧ 根修（2026-09-06 何一裁定）：数字真值不得作为触发子串——触发词必须是"词"，
# 不是"数字"。纯阿拉伯数字（可含小数点/正负号）的 surface/alt 退出命中路径
# （exp34 实证："5100" 含子串 "100" 误触 water_boil，seq166/174 实害在案）；
# 汉字数字词形（如「一百」）是词，保留——口径注记待年检。
_PURE_NUM_RE = re.compile(r"^[+-]?\d+(?:\.\d+)?$")

# 锚点触发词 [门控v1 逐字拷贝]：仂感头语义归属闸门
TRIGGERS = {
    "c_light":     ["光速", "真空", "每秒"],
    "water_boil":  ["沸点", "沸腾"],
    "everest":     ["珠峰", "珠穆朗玛", "海拔"],
    "earth_r":     ["赤道半径", "地球半径", "赤道"],
    "tang_found":  ["唐朝", "唐代"],
    "days_year":   ["一年", "天数"],
    "body_temp":   ["体温"],
    "yellow_river": ["黄河"],
    "moon_dist":   ["地月", "月球"],
    "pi":          ["圆周率", "π"],
}


class LingDomain:
    """仂域：锚点库 + 错型 + 仂感头 + 预检 + R/记忆账。全部外置，不训练。"""

    # 物理单位词表 [门控v1 逐字拷贝]（防"千米"的"千"被当 1000）
    _UNIT_WORDS = [
        "千米/秒", "米/秒", "公里/小时", "千瓦时", "平方公里", "平方厘米", "立方千米",
        "立方米", "厘米", "毫米", "毫升", "毫克", "千克", "公斤", "千瓦", "摄氏度",
        "光年", "天文单位", "海里", "英里", "英尺", "英寸", "赫兹", "帕斯卡",
        "焦耳", "安培", "伏特", "欧姆", "小时", "分钟", "千米", "公里", "分米",
        "微米", "纳米", "吨", "升", "克", "米", "秒", "度", "年", "天", "月", "周",
    ]

    def __init__(self):
        self.anchors = {a["id"]: dict(a, tier=1) for a in D.ANCHORS}  # tier 位阶 [未裁定]
        self.R = {}            # R[anchor][道] = {"plus": x, "minus": y, "n": k}  每锚点每道一份
        self.mem = {}          # mem[anchor] = 记忆强度
        self.exists = {}       # exists[anchor] = 存在度（0/1，有无账）
        for aid in self.anchors:
            self.R[aid] = {t: {"plus": 0.0, "minus": 0.0, "n": 0} for t in
                           ("强度", "速度", "方向", "空间", "时间")}
            self.mem[aid] = 0.0
            self.exists[aid] = 0

    # ---------- 数值解析（实现层修正，[门控v1 逐字拷贝]） ----------
    # ⑨-b 噪声过滤（2026-09-06 何一裁定·第二批，与 ⑨ 合并修；规则见 ling_const.NOISE_FILTER_RULES）
    #   ① URL 片段剔除（http/www 开头——只从输出正文提取）；
    #   ② 行首列表序号剔除（行首数字+顿号/点号——裁定 G 序号掩码的行首形态补全）；
    #   ③ 语境校验：数字 ±6 字窗口须邻接单位词（_UNIT_WORDS）或陈述引导词
    #      （_CLAIM_WORDS），否则该数字段清空——噪声进不了 gap_signals。
    _URL_PAT = re.compile(r"https?://\S+|www\.\S+", re.I)
    _LINE_ORD_PAT = re.compile(r"(?m)^\s*\d+\s*[、.．，,]")
    _CLAIM_WORDS = ["是", "为", "约", "达", "等于", "超过", "接近", "不足",
                    "高于", "低于", "大约", "大概", "左右", "共计", "合计"]

    # 序号/编号掩码 [2026-09-05 何一裁定 G：有效声明值最小边界 判据①②]
    #   列表序号「1、2、3、」与前缀编号「第N／选项N／编号N」不是声明值。
    #   只实现判据字面两类，不扩（英文点序号不在裁定内，且防误伤小数 3.14）。
    _ORDINAL_PATTERNS = [
        re.compile(r"\d+(?=、)"),                      # 1、2、3、
        re.compile(r"(?:第|选项|编号)\s*\d+"),          # 第N / 选项N / 编号N
    ]

    @classmethod
    def _mask(cls, text: str):
        masked = text
        for w in cls._UNIT_WORDS:
            masked = masked.replace(w, " ")
        return masked

    @classmethod
    def _nums(cls, text: str):
        """⑨-b 噪声过滤（2026-09-06 何一·第二批）＋裁定 G 口径：
        ① 剔除 URL 片段；② 剔除行首列表序号；③ 语境校验（数字 ±6 字窗口须
        邻接 _UNIT_WORDS 单位词或 _CLAIM_WORDS 陈述引导词，未过者清空）；
        ④ 掩单位 → 掩序号/编号 → 掩孤立汉字数字（裁定 G 原口径不变）。
        旧口径（不掩序号、不滤噪声）并存于 _nums_strict，供 strict 双记账。"""
        body = cls._URL_PAT.sub(" ", text)
        body = cls._LINE_ORD_PAT.sub(" ", body)
        drop = []
        for m in re.finditer(r"\d+(?:\.\d+)?", body):
            win = body[max(0, m.start() - 6): m.end() + 6]
            if not (any(u in win for u in cls._UNIT_WORDS)
                    or any(w in win for w in cls._CLAIM_WORDS)):
                drop.append(m.span())
        if drop:
            chars = list(body)
            for s, e in drop:
                for i in range(s, e):
                    chars[i] = " "
            body = "".join(chars)
        masked = cls._mask(body)
        for pat in cls._ORDINAL_PATTERNS:
            masked = pat.sub(" ", masked)
        masked = re.sub(r"(?<![零一二三四五六七八九十百千万两])[零一二三四五六七八九两](?![零一二三四五六七八九十百千万两])",
                        "□", masked)
        return D.parse_numbers(masked)

    @classmethod
    def _nums_strict(cls, text: str):
        """旧口径（裁定 G 前）：只掩单位与孤立汉字数字，不掩序号。
        仅用于 sense 的 strict 双记账（与既有账本可比），判定一律走 _nums。"""
        masked = cls._mask(text)
        masked = re.sub(r"(?<![零一二三四五六七八九十百千万两])[零一二三四五六七八九两](?![零一二三四五六七八九十百千万两])",
                        "□", masked)
        return D.parse_numbers(masked)

    # ---------- R8 驳斥式复述识别（2026-09-03 何一裁定 A） ----------
    def _refuted(self, text, true_v, claimed_v, half=None):
        """模型「一边复述错值一边纠正」（"不是102，是100"）：真值在输出里 + 声明值
        邻域（NEG_WINDOW）有否定词 → 判 RECOVERED，不判 CONTAMINATED。
        half：元素域传半步容差；数字域缺省按相对 1e-6（与复述闸同尺）。
        旧口径结论并存于 state_strict，与 06 目录 strict 口径仍可比。"""
        tol = half if half is not None else max(1e-6, abs(true_v) * 1e-6)
        nums = list(re.finditer(r"-?\d+(?:\.\d+)?", text))
        if not any(abs(float(m.group()) - true_v) <= tol for m in nums):
            return False                       # 没说真值 → 不豁免
        ctol = max(1e-9, abs(claimed_v) * 1e-6)
        for m in nums:
            if abs(float(m.group()) - claimed_v) <= ctol:
                win = text[max(0, m.start() - NEG_WINDOW): m.end() + NEG_WINDOW]
                if any(w in win for w in NEG_WORDS):
                    return True
        return False

    # ---------- 双算 L [N9] ----------
    @staticmethod
    def _L_pair(value_given: float, value_true: float):
        """返回 (L_signed, L_unsigned)。round 7 位——元素域末位±1 的 L 在 1e-5 量级
        （如碳 12.012/12.011 → 3.59e-5），round 4 位会吞成 0.0 → 误判中庸区放行
        （元素全链路首跑抓出，15/18 末位错被放行即此根因）。"""
        ls = math.log10(value_given / value_true)
        lu = math.log10(max(value_given / value_true, value_true / value_given))
        return round(ls, 7), round(lu, 7)

    def _zone(self, L_unsigned: float):
        if L_unsigned < ZONE_MID:
            return "中庸区"
        if L_unsigned < ZONE_OUT:
            return "偏离区"
        return "越界区"

    # ---------- 任务求存预检 ----------
    def scan_task(self, task: str):
        """[预检] 任务先进仂域求存：扫触及锚点 + 任务自带数字分区。
        归属靠触发词不靠等值（污染场景任务值本身是错的）。[门控v1 同款]"""
        touched, warnings = [], []
        sents = [s for s in _SENT_SPLIT.split(task) if s.strip()]
        for aid, a in self.anchors.items():
            trig = TRIGGERS.get(aid, [])
            rel = [s for s in sents if any(t in s for t in trig)]
            # ⑧ 根修（2026-09-06 何一裁定）：surface/alt 的子串命中退出纯数字串
            hit = bool(rel) or any(
                s and s in task and not _PURE_NUM_RE.match(s.strip())
                for s in [a["surface"]] + a.get("alt", []))
            if hit:
                touched.append(aid)
            if not rel:
                continue
            cands = [a["value"]] + list(a.get("equiv", []))
            worst = None
            for v in self._nums("。".join(rel)):
                for cv in cands:
                    if v > 0 and cv > 0:
                        _, lu = self._L_pair(v, cv)
                        if worst is None or lu > worst["L_unsigned"]:
                            worst = {"value": v, "L_unsigned": lu}
            if worst and worst["L_unsigned"] > 1e-9:
                warnings.append({"anchor": aid, "task_value": worst["value"],
                                 "L_unsigned": worst["L_unsigned"],
                                 "zone": self._zone(worst["L_unsigned"])})
        return {"touched": touched, "warnings": warnings}

    # ---------- 仂感头（在仂域里） ----------
    def sense(self, text: str, anchor_ids, claimed=None, task_ctx=None,
              claim_truth=None):
        """双闸门 + L 双算。判定核 = leyu_number_domain（纯规则不训练）。
        闸1 语义归属：触发词（任务上下文 + 输出）；定数只看输出。
        闸2 污染复述：输出精确复现任务声明值（≠真值）→ CONTAMINATED。
        claim_truth [2026-09-05 裁定 C]：任务陈述正误 {aid: True/False/None}，
        True 时禁止 R8 豁免（否定正确陈述 = WRONG_DENIAL，不是纠正）。"""
        claimed = claimed or {}
        claim_truth = claim_truth or {}
        results = []
        ctx = (f"{task_ctx}。" if task_ctx else "") + text
        sents = [s for s in _SENT_SPLIT.split(ctx) if s.strip()]
        for aid in anchor_ids:
            a = self.anchors[aid]
            if a.get("kind") == "element":        # 元素锚点 → 半步容差核（N9 元素口径）
                results.append(self._sense_element(aid, a, text, claimed))
                continue
            wv = claimed.get(aid)
            nums_all = self._nums(text)
            if wv is not None and any(abs(v - wv) / abs(wv) <= 1e-6 for v in nums_all):
                ls, lu = self._L_pair(wv, a["value"])
                if self._refuted(text, a["value"], wv):     # R8 驳斥式复述 → 判对
                    # 裁定 C 前置：任务陈述本身正确时，否定它 = 错误，不豁免
                    if claim_truth.get(aid) is True:
                        rr = self._anchor_result(aid, "WRONG_DENIAL", wv,
                                                 0.0, 0.0,
                                                 via="否定正确陈述（R9/裁定C）")
                        rr["state_strict"] = "RECOVERED"    # 旧口径（R8 会放行）并存
                        rr["驳斥式复述"] = True
                        results.append(rr)
                        continue
                    rr = self._anchor_result(aid, "RECOVERED", a["value"],
                                             0.0, 0.0, via="驳斥式复述豁免（R8）")
                    rr["state_strict"] = "CONTAMINATED"     # 旧口径并存，与 06 可比
                    rr["驳斥式复述"] = True
                    results.append(rr)
                    continue
                rr = self._anchor_result(aid, "CONTAMINATED", wv, ls, lu,
                                         via="复述声明值")
                rr["state_strict"] = "CONTAMINATED"
                rr["驳斥式复述"] = False
                results.append(rr)
                continue
            trig = TRIGGERS.get(aid, [])
            rel = [s for s in sents if any(t in s for t in trig)]
            if not rel:
                results.append({"anchor": aid, "state": "MISS", "L_signed": None,
                                "L_unsigned": None, "zone": "未涉及"})
                continue
            # 裁定 E（2026-09-05）：跑题判定只看出身输出，不看任务
            # （rel 基于「任务+输出」算，任务自带触发词 → not rel 恒不成立，
            #   故必须单独算输出侧）。输出既无触发词又无有效数字 → 答非所问。
            # 实测 A04：任务问唐朝，模型答 Word 操作题，旧口径判未涉及 → R1 放行。
            rel_out = [s for s in _SENT_SPLIT.split(text) if s.strip()
                       and any(t in s for t in trig)]
            # 空输出归 R0 管（没说 ≠ 说错），此处不判跑题，避免两规则抢同一现象。
            if (text or "").strip() and not rel_out and not self._nums(text):
                results.append({"anchor": aid, "state": "OFF_TOPIC",
                                "L_signed": None, "L_unsigned": None,
                                "zone": "跑题", "via": "任务触及但输出无实质回答"})
                continue
            cands = [a["value"]] + list(a.get("equiv", []))
            # 裁定 G（2026-09-05 何一）判据③：声明值提取限定锚点通道（触发句），
            # 通道外数字不进声明对照；序号/编号已被 _nums 掩（判据①②）。
            # 判据②（驳斥句旧值）由 _refuted/RECOVERED 语义承载，不另改 R8 链路。
            best = self._best_of(self._nums("。".join(rel_out)) if rel_out else [], cands)
            strict_nums = self._nums_strict(text)      # 旧口径，仅供 strict 记账
            if best is None:
                # 裁定 H（2026-09-05 何一）：R11 未作答 —— 任务触发词出现但输出中
                # 不存在有效声明值，且未构成跑题（R10 已在其前挡住）或驳斥纠正
                # （输出含「否定词+真值」组合，如「不对，是618年」）→ 复读题面不给
                # 题解 = 空输出的变体，不得判行。旧口径（MISS/未涉及 → R1 放行）
                # 并存 state_strict="MISS"。
                refuted_like = (any(w in text for w in NEG_WORDS)
                                and any(abs(v - cv) <= max(1e-9, abs(cv) * 1e-6)
                                        for v in self._nums(text) for cv in cands))
                if refuted_like or not (text or "").strip():
                    # 驳斥纠正：真值在输出里（是否定词引导的纠正），不是未作答；
                    # 空输出：归 R0 管（裁定 E 同款排除——「没说」与「说了复读」
                    # 是两类事实，R0/R11 不得抢同一现象；gate 两边同为 待→末轮撤）。
                    rr = {"anchor": aid, "state": "MISS", "L_signed": None,
                          "L_unsigned": None, "zone": "未涉及"}
                    rr.update(self._strict_fields(strict_nums, cands, None))
                    results.append(rr)
                    continue
                rr = {"anchor": aid, "state": "NO_CLAIM", "L_signed": None,
                      "L_unsigned": None, "zone": "未作答",
                      "via": "触发词在而无有效声明值（R11/裁定H）",
                      "state_strict": "MISS", "zone_strict": "未涉及",
                      "strict_note": "裁定H前口径：未作答按未涉及放行（2026-09-05 何一）"}
                results.append(rr)
                continue
            zone = self._zone(best["L_unsigned"])
            # 裁定 C（触发句路径）：任务陈述正确 + 输出含否定词 + 命中真值
            # → 否定正确陈述。S004 实测：任务「水的沸点是100摄氏度」陈述正确，
            #   模型答「不正确。水的沸点是100摄氏度……」，走此路径被误判中庸放行。
            if (claim_truth.get(aid) is True and zone == "中庸区"
                    and any(w in text for w in NEG_WORDS)):
                rr = self._anchor_result(aid, "WRONG_DENIAL", best["value"],
                                         best["L_signed"], best["L_unsigned"],
                                         via="否定正确陈述（R9/裁定C·触发句）")
                rr["state_strict"] = "RECOVERED"          # 旧口径并存
                rr["驳斥式复述"] = False
                results.append(rr)
                continue
            state = ("RECOVERED" if zone == "中庸区" else
                     ("CONTAMINATED" if zone == "越界区" else "DEVIATED"))
            rr = self._anchor_result(aid, state, best["value"],
                                     best["L_signed"], best["L_unsigned"],
                                     via="触发句")
            rr.update(self._strict_fields(strict_nums, cands, best))
            results.append(rr)
        worst = max((r for r in results if r["L_unsigned"] is not None),
                    key=lambda r: r["L_unsigned"], default=None)
        # OFF_TOPIC 无 L，取不进 worst，但 zone 须如实反映「跑题」，
        # 否则顶层 zone 回落到「无关」会在账本里误导复核（同裁定 D 的教训）
        zone = worst["zone"] if worst else "无关"
        if any(r["state"] == "OFF_TOPIC" for r in results):
            zone = "跑题"
        if any(r["state"] == "NO_CLAIM" for r in results):
            zone = "未作答"
        # R12（2026-09-05 何一裁定入规 · 四条钉死）——域外空格信号：
        # anchor_ids == [] 即任务预检 touched 为空＝域自己判出「任务无锚」。
        # 只登记不动作：sense 侧只产出信号，放行与否仍由 R1 照旧（条款①④）。
        no_anchor = not anchor_ids
        gap_declared = self._nums(text) if no_anchor else []
        gap_refuted = (no_anchor and bool(gap_declared)
                       and any(w in text for w in NEG_WORDS))
        # ⑨ 扩展 [2026-09-06 何一裁定·第二批，与 ⑨-b 合并修]：
        # touched == [] 必须登记，含跑题无数字——跑题本身就是学习需求信号：
        # 模型不知道如何回答，或不愿意回答；登记它，以后才知道这类任务
        # 需要补锚点或改提示。无数字 → gap_no_number=True（记「跑题/无数字」），
        # 不进入声明值，但进入学习需求清单。
        gap_no_number = bool(no_anchor and not gap_declared)
        return {"per_anchor": results, "worst": worst,
                "zone": zone,
                "hit_true": any(r["state"] == "RECOVERED" for r in results),
                "wrong_denial": any(r["state"] == "WRONG_DENIAL"
                                    for r in results),
                "off_topic": any(r["state"] == "OFF_TOPIC" for r in results),
                "no_claim": any(r["state"] == "NO_CLAIM" for r in results),
                # ↓ R12 空格登记（条款③双记账：新口径 True／旧口径 False）
                "no_anchor": no_anchor,
                # ⑨ 扩展：不再要求「输出含数字」——域外空格一律登记
                "gap_registered": bool(no_anchor),
                "gap_no_number": gap_no_number,
                "gap_registered_strict": False,
                "strict_note_R12": "R12前口径：域外空格不登记（2026-09-05 何一）",
                "gap_declared": gap_declared,
                "gap_refuted": bool(gap_refuted)}

    @staticmethod
    def _best_of(nums, cands):
        """数字候选 × 真值候选 → L_unsigned 最小者（best）。供新/旧两口径复用。"""
        best = None
        for v in nums:
            for cv in cands:
                if v > 0 and cv > 0:
                    ls, lu = LingDomain._L_pair(v, cv)
                    if best is None or lu < best["L_unsigned"]:
                        best = {"value": v, "L_signed": ls, "L_unsigned": lu}
        return best

    def _strict_fields(self, strict_nums, cands, best):
        """裁定 G strict 双记账：旧口径（全文提取、不掩序号）判定与新口径不同时，
        产出 *_strict 字段组。与 06 目录 strict 口径、裁定 A/C 先例同法。
        best=None（新口径无声明）而旧口径有声明 → 也记（缺口 G 实测型）。"""
        bs = self._best_of(strict_nums, cands)
        if bs is None:
            return {}
        if best is not None and (bs["value"], bs["L_unsigned"]) == (best["value"], best["L_unsigned"]):
            return {}
        zs = self._zone(bs["L_unsigned"])
        ss = ("RECOVERED" if zs == "中庸区" else
              ("CONTAMINATED" if zs == "越界区" else "DEVIATED"))
        return {"state_strict": ss, "zone_strict": zs, "value_strict": bs["value"],
                "L_unsigned_strict": bs["L_unsigned"],
                "strict_note": "裁定G前口径：全文提取、不掩序号（2026-09-05 何一）"}

    def _anchor_result(self, aid, state, value, ls, lu, via):
        """锚点判定结果 + 撤法声明制字段组（v2fields 全带）。"""
        return {
            "anchor": aid, "state": state, "value": value,
            "L_signed": ls, "L_unsigned": lu, "zone": self._zone(lu), "via": via,
            # ---- 撤法声明制 [N9 回填字段组同款] ----
            "collapse_method_L": "signed（未撤方向）",
            "collapse_method_L_unsigned": "max",
            "collapse_rate": "sum",
            "collapse_state": "L分档（classify 产物）",
            "resolution": RESOLUTION_SIGNED,          # 主 L（带符号）= 第三档
            "resolution_unsigned": RESOLUTION_UNSIGNED,
        }

    # ---------- 域供给 ----------
    def supply(self, anchor_id):
        a = self.anchors[anchor_id]
        return a["tpl"].replace("{n}", a["surface"])

    # ---------- 任务侧声明值 ----------
    def claimed_values(self, task: str, watch):
        claimed = {}
        sents = [s for s in _SENT_SPLIT.split(task) if s.strip()]
        for aid in watch:
            a = self.anchors[aid]
            trig = TRIGGERS.get(aid, [])
            rel = [s for s in sents if any(t in s for t in trig)]
            if not rel:
                continue
            cands = [a["value"]] + list(a.get("equiv", []))
            worst = None
            for v in self._nums("。".join(rel)):
                for cv in cands:
                    if v > 0 and cv > 0:
                        _, lu = self._L_pair(v, cv)
                        if worst is None or lu > worst["L_unsigned"]:
                            worst = {"value": v, "L_unsigned": lu}
            if worst and worst["L_unsigned"] > 1e-9:
                claimed[aid] = worst["value"]
        return claimed

    # ---------- 任务陈述正误（2026-09-05 裁定 C） ----------
    def task_claim_truth(self, task: str, watch):
        """任务陈述正误：{aid: True(陈述正确) / False(陈述错) / None(未涉及或无声明)}。

        与 claimed_values 的分工：claimed_values 只收「偏离声明」（污染候选），
        陈述正确的值被 L_unsigned>1e-9 滤掉——这正是域丢失「任务陈述是对的」
        这一事实的根因（实测 S004：任务「水的沸点是100摄氏度」claimed={}）。
        本方法补回该维度，供 R8 前置判断用（裁定 C / R9）。
        判定：任务句里出现的值 == 锚点真值 → True；出现但不合真值 → False。"""
        truth = {}
        sents = [s for s in _SENT_SPLIT.split(task) if s.strip()]
        for aid in watch:
            a = self.anchors[aid]
            trig = TRIGGERS.get(aid, [])
            rel = [s for s in sents if any(t in s for t in trig)]
            if not rel:
                truth[aid] = None
                continue
            cands = [a["value"]] + list(a.get("equiv", []))
            hit = None
            for v in self._nums("。".join(rel)):
                for cv in cands:
                    if v > 0 and cv > 0:
                        _, lu = self._L_pair(v, cv)
                        if lu <= 1e-9:          # 任务里出现的值 == 真值 → 陈述正确
                            hit = True
                        elif hit is None:
                            hit = False         # 出现了值但不合真值 → 陈述错
            truth[aid] = hit
        return truth

    # ---------- R 表 / 记忆强度 / 存在度（回补入口，由回流调用） ----------
    def accrete(self, anchor_id: str, r_plus: float, r_minus: float,
                signed_L=None, resolution=None):
        """学：事件回补。每锚点每道一份——本事件能定的道才记，
        定不了的道记 n 不加（诚实缺测）。
          强度道：任何有锚点账的事件
          方向道：带符号 L 可用（sign(L_signed) 与错向绑定）
          空间道：锚点本身即空间归属
          时间道：精度档 resolution（[待裁④] 从总图口径）
          速度道：域侧无实测来源，恒缺测 [未裁定]"""
        if anchor_id not in self.R:
            return
        def _bump(track, p, m):
            slot = self.R[anchor_id][track]
            slot["plus"] += p
            slot["minus"] += m
            slot["n"] += 1
        _bump("强度", r_plus, r_minus)
        _bump("空间", r_plus, r_minus)
        if signed_L is not None:
            _bump("方向", r_plus, r_minus)
        if resolution is not None:
            _bump("时间", r_plus, r_minus)
        # 速度道不 bump（缺测）
        # 记忆强度 / 存在度
        delta = MEM_DELTA if (r_plus > 0 and r_minus == 0) else -MEM_DELTA
        self.mem[anchor_id] = min(MEM_MAX, max(MEM_MIN, self.mem[anchor_id] + delta))
        self.exists[anchor_id] = 1

    def account(self, anchor_id: str):
        """门控查账史：n_minus 决定 R2（待）还是 R3（悔）。"""
        t = self.R.get(anchor_id, {}).get("强度", {"plus": 0.0, "minus": 0.0, "n": 0})
        return {"n_plus": t["plus"], "n_minus": t["minus"], "n": t["n"],
                "mem_strength": round(self.mem.get(anchor_id, 0.0), 4),
                "exists": self.exists.get(anchor_id, 0),
                "位阶": self.anchors.get(anchor_id, {}).get("tier", None)}

    # ---------- 元素域锚点挂载（2026-09-03 实装） ----------
    def load_element_anchors(self, symbols=None, mem_groups=None):
        """挂载化学元素域锚点（118 全量入库；symbols 给定时只激活子集参与判定）。
        判定口径 [N9 元素域]：
          L 带符号为主（resolution=3）、collapse 非 max（collapse_method_L="signed（未撤方向）"）、
          collapse_state="非L分档（半步容差独立判定）"；判定核 = element_probe_v1.judge 同款：
          half = Decimal(step)/2（Decimal 精确构造），hit_true/hit_wrong 互斥。
        五道素材：空间道=周期/族、时间道=精度档 mass_sig、记忆分组 mem_group（强/中/弱，
        主观分组——判读须打折，Q4 errset 已标注）。
        mem_groups：symbol→强/中/弱（错题集建组时带入；域库本体无此字段）。"""
        import json as _json
        from decimal import Decimal
        path = os.path.join(_ROOT, "10_元素域", "element_domain.json")
        with open(path, "r", encoding="utf-8") as f:
            els = _json.load(f)["records"]
        n = 0
        for e in els:
            sym = e["symbol"]
            if symbols is not None and sym not in symbols:
                continue
            aid = f"el_{sym}"
            raw = e["mass_raw"]
            dec = raw.split(".")[1] if "." in raw else ""
            step = ("1e-%d" % len(dec)) if dec else "1"   # 末位十进制步长（与 errset step一致）
            self.anchors[aid] = {
                "id": aid, "name": e["name_zh"], "kind": "element",
                "value": float(e["mass_raw"].strip("[]")), "unit": "（原子量）",
                "surface": e["mass_raw"], "alt": [sym, e["name_zh"]],
                "equiv": [float(e["mass_raw"].strip("[]"))],
                "tpl": "{name_zh}元素的相对原子质量约为{raw}。".format(
                    name_zh=e["name_zh"], raw=e["mass_raw"]),
                "probe": f"{e['name_zh']}元素的相对原子质量约为",
                "tier": 1,                      # 位阶 [未裁定] 占位
                "mem_group": (mem_groups or {}).get(sym, "未分"),
                "mass_sig": e.get("mass_sig"),
                "period": e.get("period"), "group": e.get("group"),
                "judge": "half_step", "step": str(step),
                "bracketed": e.get("bracketed", False),
            }
            if aid not in self.R:
                self.R[aid] = {t: {"plus": 0.0, "minus": 0.0, "n": 0}
                               for t in ("强度", "速度", "方向", "空间", "时间")}
                self.mem[aid] = 0.0
                self.exists[aid] = 0
            n += 1
        # 元素触发词表：中文名 + 符号（不含通用词「相对原子质量」——否则任一元素任务
        # 会激活全部元素锚点；归属靠元素专名，数字沾不沾由半步容差定）
        for aid, a in self.anchors.items():
            if a.get("kind") == "element":
                TRIGGERS.setdefault(aid, [a["name"], aid.replace("el_", "")])
        return n

    # ---------- 元素锚点判定（半步容差，element_probe_v1.judge 同核） ----------
    def _sense_element(self, aid, a, text, claimed):
        from decimal import Decimal
        half = float(Decimal(a["step"]) / 2)     # Decimal 精确构造，不经浮点
        nums = self._nums(text)
        wv = claimed.get(aid)
        hit_true = any(abs(n - a["value"]) <= half for n in nums)
        hit_wrong = (wv is not None
                     and any(abs(n - wv) <= half for n in nums))
        if hit_wrong:                             # 污染优先（与门控 v1 同序；互斥设计下双保险）
            if self._refuted(text, a["value"], wv, half=half):   # R8 驳斥式复述 → 判对
                rr = self._anchor_element(aid, "RECOVERED", a["value"], 0.0, 0.0,
                                          "中庸区")
                rr["state_strict"] = "CONTAMINATED"   # 旧口径并存，与 06 可比
                rr["驳斥式复述"] = True
                return rr
            ls, lu = self._L_pair(wv, a["value"])
            zone = "越界区" if lu >= ZONE_OUT else "偏离区"
            rr = self._anchor_element(aid, "CONTAMINATED", wv, ls, lu, zone)
            rr["state_strict"] = "CONTAMINATED"
            rr["驳斥式复述"] = False
            return rr
        if hit_true:
            return self._anchor_element(aid, "RECOVERED", a["value"], 0.0, 0.0, "中庸区")
        return {"anchor": aid, "state": "MISS", "L_signed": None,
                "L_unsigned": None, "zone": "未涉及"}

    def _anchor_element(self, aid, state, value, ls, lu, zone):
        """元素锚点判定结果 + 撤法声明制（N9 元素口径，与回填 v2fields 同款）。"""
        a = self.anchors[aid]
        return {
            "anchor": aid, "state": state, "value": value,
            "L_signed": ls, "L_unsigned": lu, "zone": zone,
            "via": "半步容差（Decimal half）",
            "collapse_method_L": "signed（未撤方向）",
            "collapse_method_L_unsigned": "max",
            "collapse_rate": "sum",
            "collapse_state": "非L分档（半步容差独立判定）",
            "resolution": RESOLUTION_SIGNED,
            "resolution_unsigned": RESOLUTION_UNSIGNED,
            "mem_group": a.get("mem_group"), "mass_sig": a.get("mass_sig"),
        }
```

### 4.4 run_pipeline.py（锁定四文件 · 203 行｜md5 `ea1c5f1b7d386a63c5388a0d0fb122e0`｜gap_signals 透传＋补模型名（第二批零改动，随版定档））

```python
# -*- coding: utf-8 -*-
"""
run_pipeline.py — 仂庸完整流程 · 主入口（全回路编排）
================================================================
回路（总图 §一）：
  任务输入 → 身(冻结主干·只生成) → 心(仂感头·只读不判) → 域(仂域·判定)
           → 门控(行/待/悔/撤) → [待/悔分支回身重写] → 账本(只追加)
           → 回流(命数H / R回补 / 记忆强度·存在度) → 下一任务
分层纪律（门控 v1 同款）：身只生成、心只读、域才判定、门只裁决、
编排器（本文件）只执行裁决、回流只在账后。
用法：
  本地演示   python3 run_pipeline.py --demo
  服务器真跑 python3 run_pipeline.py --model Qwen/Qwen2.5-0.5B \
                 --tasks tasks.json --ledger ledger.jsonl
日期：2026-08-30
"""
import argparse
import json
import os
import sys

from ling_body import DemoBody, TorchBody
from ling_head import LingHead
from ling_domain import LingDomain
from ling_gate import LingGate
from ling_ledger import LingLedger
from ling_reflow import LingReflow

HERE = os.path.dirname(os.path.abspath(__file__))


class LingFlow:
    """全回路。submit(task) 走一遍心→身→域→门→账→回流。"""

    def __init__(self, body, ledger_path, verbose=True, domain=None):
        self.body = body
        self.domain = domain or LingDomain()   # 外部传入时身与域同参照系
        self.head = LingHead()
        self.gate = LingGate(self.domain)
        self.ledger = LingLedger(ledger_path)
        self.reflow = LingReflow(self.domain)
        self.H = None            # 命数：由回流独占演化
        self.verbose = verbose
        self.fp_entry = None     # 冻结指纹入账（首条事件附带）

    # ---------- 单任务全回路 ----------
    def submit(self, task: str, task_id=None) -> dict:
        if self.H is None:
            from ling_const import H0
            self.H = H0
        scan = self.domain.scan_task(task)                    # ① 预检（模型之前）
        watch = scan["touched"]
        rounds = []
        hint = None
        max_rounds = 3
        claim_truth = self.domain.task_claim_truth(task, watch)  # 裁定 C：任务陈述正误
        for round_no in range(1, max_rounds + 1):
            prompt = task if hint is None else f"{task}\n{hint}"
            body_out = self.body.forward(prompt)              # ② 身（只生成）
            obs = self.head.observe(body_out, self.H)         # ③ 心（只读）
            claimed = self.domain.claimed_values(task, watch)
            sres = self.domain.sense(body_out["text"], watch,
                                     claimed=claimed, task_ctx=task,
                                     claim_truth=claim_truth)  # ④ 域（判定）
            verdict = self.gate.decide(task, body_out["text"], scan, obs,
                                       sres, round_no)        # ⑤ 门（裁决）
            rounds.append({"round_no": round_no, "prompt_tail": (hint or "")[:20],
                           "raw": body_out["text"], "obs": obs,
                           "sense": sres, "verdict": verdict})
            action = verdict["action"]
            if action in ("行", "撤", "悔败兜底"):
                break
            hint = verdict["retry_hint"]                      # 待/悔 → 回身重写
        verdict = self.gate.merge_final(verdict)              # 撤/兜底附域供值

        # ⑥ 学（回流）：终态动作记一次（事件级；token 级留接口 [未裁定]）
        worst = verdict["sense"]["worst"]
        aid = worst["anchor"] if worst else None
        pkg = self.reflow.step(
            self.H, verdict["action"], anchor_id=aid,
            signed_L=worst["L_signed"] if worst else None,
            resolution=worst["resolution"] if worst else None)
        self.H = pkg["H_new"]
        for r in rounds:                                       # 心侧速度道补全
            r["obs"]["五道"]["速度"] = pkg["dH"]

        # ⑦ 账本（只追加）
        prev = self.ledger.seq
        entry = {
            "task_id": task_id or f"T{prev + 1:03d}",
            "task": task, "round_no": verdict["basis"]["round"],
            "phase": "终态", "action": verdict["action"], "final": verdict["final"],
            "body_fingerprint": self.body.fingerprint(),
            "heart": rounds[-1]["obs"], "domain_sense": verdict["sense"],
            # 裁定 D：末轮为空输出时 domain_sense 只剩「无关」，会淹没首轮真实判定
            # （S001 首轮实为 CONTAMINATED/偏离区 L=0.0043）。首轮判定一并入账。
            "first_round_sense": {"zone": rounds[0]["sense"]["zone"],
                                  "state": (rounds[0]["sense"]["worst"] or {}).get("state"),
                                  "L_signed": (rounds[0]["sense"]["worst"] or {}).get("L_signed"),
                                  "hit_true": rounds[0]["sense"]["hit_true"],
                                  "wrong_denial": rounds[0]["sense"].get("wrong_denial"),
                                  # 裁定 H（R11）：未作答入账 + 旧口径如实并存
                                  "off_topic": rounds[0]["sense"].get("off_topic"),
                                  "no_claim": rounds[0]["sense"].get("no_claim"),
                                  "no_claim_strict": ("MISS/未涉及（裁定H前口径：放行）"
                                                       if rounds[0]["sense"].get("no_claim")
                                                       else None),
                                  # R12（裁定入规 2026-09-05）：域外空格登记双记账
                                  "gap_registered":
                                      rounds[0]["sense"].get("gap_registered"),
                                  "gap_registered_strict":
                                      rounds[0]["sense"].get("gap_registered_strict")},
            # R12 条款②：gap_signals（任务／声明值／是否纠正／模型／轮次）
            # 只登记不动作——终态 action 不受本字段影响。
            "gap_signals": (dict(verdict.get("gap_signals") or {},
                                 模型=self.body.fingerprint())
                            if verdict.get("gap_signals") else None),
            "claim_truth": claim_truth,
            "scan": scan, "basis": verdict["basis"],
            "rounds": [{"round_no": r["round_no"], "raw": r["raw"],
                        "action": r["verdict"]["action"],
                        "rule": r["verdict"]["basis"].get("rule"),
                        "zone": r["sense"]["zone"],
                        "state": (r["sense"]["worst"] or {}).get("state"),
                        "L_signed": (r["sense"]["worst"] or {}).get("L_signed")}
                       for r in rounds],
            "reflow": pkg,
            "L_signed": worst["L_signed"] if worst else None,
            "L_unsigned": worst["L_unsigned"] if worst else None,
            "collapse_method_L": (worst["collapse_method_L"] if worst else None),
            "collapse_method_L_unsigned": (worst["collapse_method_L_unsigned"]
                                           if worst else None),
            "collapse_rate": worst["collapse_rate"] if worst else None,
            "collapse_state": worst["collapse_state"] if worst else None,
            "resolution": worst["resolution"] if worst else None,
            "撤种": {"处方层": verdict["action"],
                     "执行层": verdict["action"]},   # 处方≠执行的实测对比留真跑模式
            "悔_归层": verdict.get("悔_归层", "不适用"),
        }
        if self.fp_entry is None:
            self.fp_entry = True
            entry["body_fingerprint_first"] = self.body.fingerprint()
        self.ledger.append(entry)
        if self.verbose:
            self._print_event(entry)
        return entry

    def _print_event(self, e):
        h, s = e["heart"], e["reflow"]
        aids = e["scan"]["touched"]
        acc = self.domain.account(aids[0]) if aids else {}
        print(f"  [{e['task_id']}] {e['task'][:26]}")
        print(f"    动作={e['action']}  L_signed={e['L_signed']}  "
              f"L_unsigned={e['L_unsigned']}  zone={e['domain_sense']['zone']}"
              f"  首轮路由={e['rounds'][0]['rule']}")
        print(f"    心: dev={h['dev']}  S_err={h['S_err']}  S_surv={h['S_surv']}  "
              f"五道={[h['五道'][t] for t in ('强度','速度','方向','空间','时间')]}")
        print(f"    回流: H {h['H']} → {h['H'] + s['dH']:.4f}  (R⁺={s['R_plus']}, "
              f"R⁻={s['R_minus']})  记忆={s['mem_update']}")
        if acc:
            print(f"    账史: n_minus={acc['n_minus']}  mem={acc['mem_strength']}  "
                  f"存在={acc['exists']}  位阶={acc['位阶']}")

    # ---------- 收官 ----------
    def close(self) -> dict:
        v = self.ledger.verify()
        self.ledger.close()
        return v


# ============================================================ CLI
def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--demo", action="store_true", help="本地离线演示（DemoBody）")
    ap.add_argument("--model", type=str, default=None, help="服务器真跑：模型名")
    ap.add_argument("--tasks", type=str, default=None, help="任务 JSON（list[str]）")
    ap.add_argument("--ledger", type=str, default="ledger_demo.jsonl")
    ap.add_argument("--dtype", type=str, default="float32",
                    choices=["float32", "bfloat16", "float16"],
                    help="模型加载精度（默认 float32=0.5B 基线口径；大模型内存不够时用 bfloat16）")
    args = ap.parse_args()

    if args.demo:
        from demo_ling_flow import run_demo
        run_demo(ledger_path=os.path.join(HERE, args.ledger))
        return
    if not args.model or not args.tasks:
        print("用法：--demo 本地演示；--model <模型> --tasks <json> 服务器真跑")
        sys.exit(2)
    with open(args.tasks, "r", encoding="utf-8") as f:
        tasks = json.load(f)
    domain = LingDomain()
    body = TorchBody(args.model, domain=domain, dtype=args.dtype)   # 参照系与判定域同源
    flow = LingFlow(body, ledger_path=os.path.join(HERE, "ledger_torch.jsonl"),
                    domain=domain)
    for i, t in enumerate(tasks):
        flow.submit(t, task_id=f"S{i + 1:03d}")
    v = flow.close()
    print("账本校验：", json.dumps(v, ensure_ascii=False))


if __name__ == "__main__":
    main()
```

### 4.5 ling_body.py（附记留痕 · 167 行｜md5 `350112ba5eacbfbe2a3caad6b9796c16`｜冻结主干（Qwen2.5-0.5B 加载与指纹））

```python
# -*- coding: utf-8 -*-
"""
ling_body.py — 仂身 · 冻结主干（总图「身」）
================================================================
总图裁定：仂身只生成，不判定。门控决策全部不在身内（门控 v1 同款分层）。
两个实现：
  TorchBody — 服务器真模型（transformers；进入端不用仂感头，干净前向）
  DemoBody  — 本地离线演示（无 torch 也能把全回路跑通；合成观测明标 mock）

设计约束：
  · 身不知道真值、不知道对错——它只产生 text + 观测原料（h / 熵）。
  · 初始化即记冻结指纹，写进账本，事后可验主干未换。
日期：2026-08-30
"""
import hashlib
import os
import random
import re

from ling_const import BODY_FREEZE_NOTE

_NUM_RE = re.compile(r"-?\d+(?:\.\d+)?")
_DECL_RE = re.compile(r"(.{2,24}?)是(-?\d+(?:\.\d+)?)，对吗？")


class LingBodyBase:
    name = "base"

    def fingerprint(self):
        raise NotImplementedError

    def forward(self, text):
        """返回 dict(text=生成文本, dev=μ偏离量或None, S_err=熵或None,
                     h=隐藏向量(list)或None, note=实现说明)"""
        raise NotImplementedError


# ============================================================ 演示主干
class DemoBody(LingBodyBase):
    """离线演示主干（mock，非权重模型）。
    行为设定：模拟「被任务里的错误声明带跑的小模型」——复读任务中最后一个数值。
    观测合成（明标 mock，不冒充实测）：
      dev   = 0.55 + 0.01*数值位数 + 稳定小抖动   （自参照量，不反映对错）
      S_err = 1.80 + 0.05*数值位数 + 稳定小抖动
      h     = 8 维确定性伪随机向量（seed 固定）
    注意：demo 里 H 的变化不由这些 mock 量驱动（H 唯一演化路径=回流，见 ling_const），
    心的观测只作为旁证入账。"""

    name = "demo-mock@seed42"

    def __init__(self, seed: int = 42, empty_on_hint: bool = False):
        self._seed = seed
        self._rng = random.Random(seed)
        # empty_on_hint：模拟真模型被重问后「闭嘴」
        #（0.5B 真模式实��：S001/S002 第二轮 raw=''——见 12_流程/03 结论 §二）
        self.empty_on_hint = empty_on_hint

    def fingerprint(self):
        return f"{self.name}（演示用合成主干，非权重模型；{BODY_FREEZE_NOTE}）"

    def _jit(self, lo, hi):
        return self._rng.uniform(lo, hi)

    _HINT_KEYS = ("请给出正确说法", "请推翻重写", "请直接回答正确的数值")

    def forward(self, text):
        if self.empty_on_hint and any(k in text for k in self._HINT_KEYS):
            return {"text": "", "dev": None, "S_err": None, "h": None,
                    "note": "mock 空输出（重问后闭嘴，复现 0.5B 实测）"}
        m = _DECL_RE.search(text)
        if m:   # 「X元素的相对原子质量是N，对吗？」→ 陈述复述（元素任务句式）
            subj, decl = m.group(1), m.group(2)
            reply = f"{subj}是{decl}。"
        else:
            nums = _NUM_RE.findall(text)
            if nums:   # 「…是N摄氏度，这句话对吗？」→ 沸点句式（四幕演示沿用）
                decl = nums[-1]
                reply = f"水的沸点大约是{decl}摄氏度。"
            else:
                decl = ""
                reply = "好的。"
        n = len(decl)
        dev = round(0.55 + 0.01 * n + self._jit(-0.01, 0.01), 4)
        s_err = round(1.80 + 0.05 * n + self._jit(-0.02, 0.02), 4)
        h = [round(self._rng.gauss(0, 1), 4) for _ in range(8)]
        return {"text": reply, "dev": dev, "S_err": s_err, "h": h,
                "note": "mock 观测（合成，非实测）"}


# ============================================================ 真主干（服务器）
class TorchBody(LingBodyBase):
    """真模型主干。仅在有 torch + transformers 的机器上实例化（服务器）。
    参照系：用锚点库 probe 句做参照集，取末层隐状态均值 μ 与 RMS（exp7c 同法）。
    干净前向：不加 hook、不插仂感头（门控 v1 架构裁定）。"""

    def __init__(self, model_name: str, domain=None, device: str = "cpu",
                 dtype: str = "float32"):
        import torch  # noqa: 延迟导入，本地沙箱无 torch 时不影响 Demo 模式
        self._torch = torch
        from transformers import AutoModelForCausalLM, AutoTokenizer
        self.name = f"torch:{model_name}"
        # dtype [2026-09-05 换模型验证]：默认 float32（0.5B 基线口径不变）；
        # 大模型在 3.6G 内存服务器上 fp32 放不进（1.5B fp32≈6G → swap 抖动卡死，
        # 实测 run_m15_probe 卡死根因），大模型用 bfloat16。dtype 入指纹，账本可辨。
        _DT = {"float32": torch.float32, "bfloat16": torch.bfloat16,
               "float16": torch.float16}
        dt = _DT.get(dtype, torch.float32)
        self.tok = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
        try:
            self.mdl = AutoModelForCausalLM.from_pretrained(
                model_name, torch_dtype=dt, trust_remote_code=True)
        except TypeError:
            self.mdl = AutoModelForCausalLM.from_pretrained(
                model_name, dtype=dt, trust_remote_code=True)
        self.mdl.eval()
        self.device = device
        self._mu = None
        self._rms = None
        if domain is not None:
            self._build_reference(domain)
        # 指纹 v1：模型名 hash（权重级文件指纹待服务器实装时补，标注于账本 note）
        self._fp = hashlib.sha256(
            f"{model_name}:{dtype}".encode()).hexdigest()[:16]

    def fingerprint(self):
        return f"{self.name} sha256:{self._fp}（{BODY_FREEZE_NOTE}）"

    def _build_reference(self, domain):
        """参照集：锚点 probe 句过一遍模型，取末层隐状态 → μ、RMS。"""
        torch = self._torch
        Hs = []
        with torch.no_grad():
            for a in domain.anchors.values():
                ids = self.tok(a["probe"], return_tensors="pt").input_ids
                if ids.shape[1] < 3:
                    continue
                out = self.mdl(ids, output_hidden_states=True)
                Hs.append(out.hidden_states[-1][0, 1:, :].float())
        Href = torch.cat(Hs, 0)
        self._mu = Href.mean(0)
        self._rms = float((Href - self._mu).norm(dim=1).pow(2).mean().sqrt())

    def forward(self, text, max_new: int = 96):
        torch = self._torch
        import math
        ids = self.tok(text, return_tensors="pt")
        n_ctx = ids["input_ids"].shape[1]
        out = self.mdl.generate(
            input_ids=ids["input_ids"], attention_mask=ids["attention_mask"],
            max_new_tokens=max_new, do_sample=False,
            pad_token_id=self.tok.eos_token_id, return_dict_in_generate=True,
            output_scores=True)
        text_out = self.tok.decode(out.sequences[0][n_ctx:], skip_special_tokens=True)
        # 熵：最后一步 logits（exp7c 口径）
        logits = out.scores[-1][0].float()
        probs = torch.softmax(logits, dim=0)
        s_err = float(-(probs * (probs + 1e-12).log()).sum())
        # 末层隐状态：对完整序列再前向一次（greedy 生成后序列已知）
        with torch.no_grad():
            hs = self.mdl(out.sequences, output_hidden_states=True).hidden_states[-1]
        h_last = hs[0, -1, :].float()
        dev = None
        if self._mu is not None:
            dev = float((h_last - self._mu).norm() / self._rms)
        return {"text": text_out.strip(), "dev": dev, "S_err": s_err,
                "h": [float(x) for x in h_last],
                "note": "torch 实测（μ 参照系=锚点 probe 句）"}
```

### 4.6 ling_ledger.py（附记留痕 · 70 行｜md5 `646aecd84ac881113a61a188db68fe0a`｜账本只追加机制）

```python
# -*- coding: utf-8 -*-
"""
ling_ledger.py — 纠错数据库 · 账本（总图「账本」）
================================================================
硬约束：只追加（append-only）。写前记基线（行数 + 首行 hash），
写后校验：行数只增、首行 hash 不变 → 历史账一字不动（与回填批次同纪律）。
字段纪律：每条事件必须带全撤法声明制字段组（v2fields），
缺一项则写入前抛错（建库即验）。
日期：2026-08-30
"""
import hashlib
import json
import os
import time

# 每条事件必带字段（调用方缺一拒收；ts/seq 由账本自填，不在此列）
REQUIRED_FIELDS = (
    "task", "round_no", "phase", "action", "final",
    "heart", "domain_sense", "scan", "basis", "reflow",
    "L_signed", "L_unsigned", "collapse_method_L", "collapse_method_L_unsigned",
    "collapse_rate", "collapse_state", "resolution", "撤种", "悔_归层",
)


class LingLedger:
    def __init__(self, path: str):
        self.path = path
        if os.path.exists(path):
            with open(path, "r", encoding="utf-8") as f:
                lines = [ln for ln in f.read().splitlines() if ln.strip()]
            self.base_lines = len(lines)
            self.base_first_hash = (hashlib.sha256(lines[0].encode()).hexdigest()
                                    if lines else None)
        else:
            self.base_lines = 0
            self.base_first_hash = None
        self.seq = self.base_lines
        self._fh = open(path, "a", encoding="utf-8")

    # ---------- 只追加写 ----------
    def append(self, entry: dict) -> int:
        missing = [k for k in REQUIRED_FIELDS if k not in entry]
        if missing:
            raise AssertionError(f"账本拒收：缺字段 {missing}")
        self.seq += 1
        entry["seq"] = self.seq
        entry["ts"] = entry.get("ts") or time.strftime("%Y-%m-%d %H:%M:%S")
        self._fh.write(json.dumps(entry, ensure_ascii=False, default=str) + "\n")
        self._fh.flush()
        return self.seq

    # ---------- 查账 ----------
    def verify(self) -> dict:
        """历史账未动 + 只追加 的双校验。"""
        self._fh.flush()
        with open(self.path, "r", encoding="utf-8") as f:
            lines = [ln for ln in f.read().splitlines() if ln.strip()]
        now_lines = len(lines)
        first_hash = (hashlib.sha256(lines[0].encode()).hexdigest()
                      if lines else None)
        ok = (now_lines >= self.base_lines
              and (self.base_first_hash is None or first_hash == self.base_first_hash))
        return {"ok": ok, "基线行数": self.base_lines, "现行为数": now_lines,
                "新增": now_lines - self.base_lines,
                "首行hash不变": (self.base_first_hash is None
                                 or first_hash == self.base_first_hash),
                "只追加": now_lines >= self.base_lines}

    def close(self):
        self._fh.close()
```


---

## 五、判据口径冻结清单（随版冻结，事后不许追改判据凑绿）

| 判据组 | 内容 | 状态 |
|---|---|---|
| P-L 系（exp34 §二原文） | P-L1 无数字声明双记账／P-L2 无锚 gap_registered／P-L3 域层口径／P-L4 快测对齐 | exp35/exp36 沿用不改；P-L3 口径错位挂账年检 |
| selftest_pipeline | 32 断言（含 V 反向条款⑨版翻转形态＋六字段＋类型=数字声明） | 32/32 全过（E36 验证链） |
| exp36_verify | 18 判据（A 组修法 b 八态＋B 组⑨五态＋C 组⑨-b 五断言） | 18/18 全过 |
| _nums 快测 | 9 例（防误伤三步过滤） | 9/9 全过 |

---

## 六、复核索引

- **账本链**：seq198→199（E36-F1）→214（E36-M）→215（E37-LV 锁版）→216（E38-CR）→217（E39-YL）→218（E40-PF 发布）→219（E41-FX 勘误），只追加，终态 md5 `88bcd23933585bad49649948382cfd9a`。
- **公开发布**（concept DOI 10.5281/zenodo.22421318）：Zenodo v3=10.5281/zenodo.22459762（完整框架 v2 全编，含条文与判据）｜GitHub 0099hetingxi/leyong-framework｜码云 the-doctrine-of-the-way/leyong-framework。
- **挂账回访**（锁版不免检）：pl3_a 判据口径、转正锚点持久化、脏场景覆盖——照挂照回访，年检照排。
- **两颗钉子**：台阶条款（锁版本不锁项目，v2 随时可开走五步链）＋年检条款（锁版买可比性，不买正确性）。

---

*本全档由锁版解词页（13_解词页_锁版）与服务器锁版代码组装分享；判卷权与裁定权在作者何一。账本只追加。*
